# What the running campaign actually has to take. The clock used to give a day
# count and nothing else, so the Northwest campaign in particular read as having
# no completion condition at all.

defined_text = {
	name = GetVINCampaignObjective
	text = {
		trigger = { check_variable = { VIN_camp_current = 1 } }
		localization_key = VIN_CAMP_OBJ_NORTHWEST
	}
	text = {
		trigger = { check_variable = { VIN_camp_current = 2 } }
		localization_key = VIN_CAMP_OBJ_HOA_BINH
	}
	text = {
		trigger = { check_variable = { VIN_camp_current = 3 } }
		localization_key = VIN_CAMP_OBJ_DIEN_BIEN
	}
	text = {
		trigger = { check_variable = { VIN_camp_current = 4 } }
		localization_key = VIN_CAMP_OBJ_CAO_BAC
	}
	text = {
		localization_key = VIN_CAMP_OBJ_NONE
	}
}
