# Legacy lowercase GDP graph storage is no longer used by the finance UI.
clear_legacy_gdp_graph = {
	clear_array = gdp_graph
	clear_array = gdp_graphx
	clear_array = gdp_year
	clear_array = gdp_array
	clear_array = gdp_growth_array
	clear_array = date_scale
}

calculate_gdp_growth = {
	set_variable = { gdp_growth_prev = gdpbase }
	set_variable = { gdp_growth_curr = grossdomesticproduct_fake }
	subtract_from_variable = { gdp_growth_curr = gdp_growth_prev }
	if = {
		limit = {
			NOT = {
				check_variable = { gdpbase = 0 }
			}
		}
		divide_variable = { gdp_growth_curr = gdpbase }
		multiply_variable = { gdp_growth_curr = 100 }
	}
	else = {
		set_variable = { gdp_growth_curr = 0 }
	}
	set_variable = { gdpbase = grossdomesticproduct_fake }
}
