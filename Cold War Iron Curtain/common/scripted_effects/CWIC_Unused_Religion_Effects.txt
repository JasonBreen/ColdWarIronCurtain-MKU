theocracy_drift = {
	if = {
		limit = { has_country_flag = PR_ChristianityCatholic }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.TheocracyDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionChristianityCatholic = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_ChristianityProtestant }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.TheocracyDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionChristianityProtestant = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_ChristianityOrthodox }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.TheocracyDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionChristianityOrthodox = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_ChristianityOriental }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.TheocracyDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionChristianityOriental = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_Judaism }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.TheocracyDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionJudaism = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_IslamSunni }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.TheocracyDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionIslamSunni = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_IslamShia }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.TheocracyDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionIslamShia = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_IslamIbadi }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.TheocracyDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionIslamIbadi = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_BuddhismVajrayana }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.TheocracyDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionBuddhismVajrayana = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_BuddhismMahayana }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.TheocracyDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionBuddhismMahayana = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_BuddhismThereveda }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.TheocracyDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionBuddhismThereveda = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_Shinto }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.TheocracyDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionShinto = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_TraditionalEastern }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.TheocracyDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionTraditionalEastern = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_Confucian }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.TheocracyDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionConfucian = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_Hindu }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.TheocracyDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionHindu = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_Sikh }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.TheocracyDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionSikh = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_Navayana }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.TheocracyDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionNavayana = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_TraditionalIranian }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.TheocracyDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionTraditionalIranian = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_ZionistChristian }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.TheocracyDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionZionistChristian = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_CultistSafe }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.TheocracyDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionCultistSafe = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_CultistFatal }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.TheocracyDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionCultistFatal = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_TraditionalAfrican }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.TheocracyDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionTraditionalAfrican = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_TraditionalAmerican }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.TheocracyDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionTraditionalAmerican = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_TraditionalOceanic }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.TheocracyDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionTraditionalOceanic = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_ChineseSalvationist }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.TheocracyDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionChineseSalvationist = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_Messianic }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.TheocracyDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionMessianic = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_Other }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.TheocracyDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionOther = driftVar }
			normalize_religion = yes
		}
	}
}
state_religion_drift = {
	if = {
		limit = { has_country_flag = PR_ChristianityCatholic }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.State_ReligionDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionChristianityCatholic = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_ChristianityProtestant }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.State_ReligionDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionChristianityProtestant = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_ChristianityOrthodox }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.State_ReligionDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionChristianityOrthodox = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_ChristianityOriental }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.State_ReligionDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionChristianityOriental = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_Judaism }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.State_ReligionDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionJudaism = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_IslamSunni }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.State_ReligionDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionIslamSunni = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_IslamShia }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.State_ReligionDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionIslamShia = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_IslamIbadi }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.State_ReligionDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionIslamIbadi = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_BuddhismVajrayana }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.State_ReligionDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionBuddhismVajrayana = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_BuddhismMahayana }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.State_ReligionDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionBuddhismMahayana = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_BuddhismThereveda }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.State_ReligionDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionBuddhismThereveda = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_Shinto }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.State_ReligionDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionShinto = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_TraditionalEastern }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.State_ReligionDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionTraditionalEastern = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_Confucian }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.State_ReligionDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionConfucian = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_Hindu }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.State_ReligionDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionHindu = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_Sikh }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.State_ReligionDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionSikh = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_Navayana }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.State_ReligionDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionNavayana = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_TraditionalIranian }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.State_ReligionDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionTraditionalIranian = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_ZionistChristian }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.State_ReligionDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionZionistChristian = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_CultistSafe }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.State_ReligionDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionCultistSafe = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_CultistFatal }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.State_ReligionDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionCultistFatal = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_TraditionalAfrican }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.TheocracyDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionTraditionalAfrican = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_TraditionalAmerican }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.TheocracyDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionTraditionalAmerican = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_TraditionalOceanic }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.TheocracyDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionTraditionalOceanic = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_ChineseSalvationist }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.TheocracyDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionChineseSalvationist = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_Messianic }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.State_ReligionDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionMessianic = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_Other }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.State_ReligionDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionOther = driftVar }
			normalize_religion = yes
		}
	}
}
moralist_drift = {
	if = {
		limit = { has_country_flag = PR_ChristianityCatholic }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.MoralistDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionChristianityCatholic = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_ChristianityProtestant }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.MoralistDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionChristianityProtestant = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_ChristianityOrthodox }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.MoralistDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionChristianityOrthodox = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_ChristianityOriental }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.MoralistDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionChristianityOriental = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_Judaism }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.MoralistDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionJudaism = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_IslamSunni }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.MoralistDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionIslamSunni = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_IslamShia }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.MoralistDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionIslamShia = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_IslamIbadi }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.MoralistDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionIslamIbadi = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_BuddhismVajrayana }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.MoralistDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionBuddhismVajrayana = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_BuddhismMahayana }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.MoralistDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionBuddhismMahayana = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_BuddhismThereveda }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.MoralistDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionBuddhismThereveda = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_Shinto }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.MoralistDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionShinto = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_TraditionalEastern }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.MoralistDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionTraditionalEastern = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_Confucian }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.MoralistDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionConfucian = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_Hindu }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.MoralistDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionHindu = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_Sikh }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.MoralistDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionSikh = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_Navayana }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.MoralistDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionNavayana = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_TraditionalIranian }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.MoralistDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionTraditionalIranian = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_ZionistChristian }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.MoralistDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionZionistChristian = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_CultistSafe }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.MoralistDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionCultistSafe = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_CultistFatal }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.MoralistDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionCultistFatal = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_TraditionalAfrican }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.TheocracyDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionTraditionalAfrican = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_TraditionalAmerican }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.TheocracyDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionTraditionalAmerican = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_TraditionalOceanic }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.TheocracyDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionTraditionalOceanic = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_ChineseSalvationist }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.TheocracyDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionChineseSalvationist = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_Messianic }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.MoralistDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionMessianic = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_Other }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.MoralistDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionOther = driftVar }
			normalize_religion = yes
		}
	}
}
atheist_drift = {
	
	every_controlled_state = {
		set_temp_variable = { AtheistDrift = global.AtheistDrift }
		multiply_temp_variable = { AtheistDrift = 0.75 }
		multiply_temp_variable = { AtheistDrift = state_population_k }
		add_to_variable = { ReligionAtheist = AtheistDrift }
		
		set_temp_variable = { IrreligiousDrift = global.AtheistDrift }
		multiply_temp_variable = { IrreligiousDrift = 0.25 }
		multiply_temp_variable = { IrreligiousDrift = state_population_k }
		add_to_variable = { ReligionIrreligious = IrreligiousDrift }
		
		normalize_religion = yes
	}
}
