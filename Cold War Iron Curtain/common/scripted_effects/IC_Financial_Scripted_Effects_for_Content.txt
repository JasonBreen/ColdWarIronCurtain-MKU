#Needed Inputs Variables
# - Length (current_index) 0 = 5 Years, 1 = 7 Years, 2 = 10 years
# - Amount (pulse_deficit)

#Example
#set_variable = { 
#    current_index = 2
#}
#set_variable = {
#    pulse_deficit = 4
#}
#generate_reserve_debt = yes

generate_reserve_debt = {
    set_variable = {
        bank_name_prompt = 1
    }
    save_global_event_target_as = this_loan_country
    clear_array = purchase_length
    add_to_array = {
        purchase_length = 5
    }
    add_to_array = {
        purchase_length = 7
    }
    add_to_array = {
        purchase_length = 10
    }
    clear_array = currentInterestRateCalc
    add_to_array = {
        currentInterestRateCalc = ReserveCurrentInterestRate
    }
    add_to_array = {
        currentInterestRateCalc = ReserveCurrentInterestRate
    }
    add_to_array = {
        currentInterestRateCalc = ReserveCurrentInterestRate
    }
    set_variable = {
        max_index = 2
    }
    set_variable = {
        current_index = 0
    }
    automatic_loan_setting = yes
}