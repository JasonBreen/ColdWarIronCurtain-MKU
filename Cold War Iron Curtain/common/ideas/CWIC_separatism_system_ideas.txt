ideas = {
	hidden_ideas = {
		# Master switch for passive separatism growth.
		# calculate_separatism (separatism_effects.txt) and the MNC state backlash
		# (CWIC_MNC_Effects.txt) only INCREASE state separatism if the country has
		# this idea. Without it separatism can only fall (or be changed by explicit
		# scripted effects/events, which are not gated).
		# Grant via: add_ideas = CWIC_separatism_growth_enabled
		CWIC_separatism_growth_enabled = {
			allowed = { always = no }
			allowed_civil_war = { always = yes }
			modifier = { }
		}
	}
}
