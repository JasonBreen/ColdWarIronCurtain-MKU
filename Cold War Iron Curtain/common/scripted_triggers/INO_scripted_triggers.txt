has_pki_majority_party = {
    OR = {
        communism > 0.5
        maoism > 0.5
        trotskyism > 0.5
    }
    custom_trigger_tooltip = {
        tooltip = PKI_MAJORITY_TT
        always = yes
    }
}
has_pki_majority_party = {
    OR = {
        communism > 0.5
        maoism > 0.5
        trotskyism > 0.5
    }
    custom_trigger_tooltip = {
        tooltip = PKI_MAJORITY_TT
        always = yes
    }
}