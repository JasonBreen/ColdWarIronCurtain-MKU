INO_Maluku = {
	allowed = { tag = INO }
	enable = { 
		NOT = {
			owns_state = 668
		}
	}
	abort = { always = no }
	abort_when_not_enabled = yes
	
	ai_strategy = {
		type = invasion_unit_request
		state = 668
		value = 200
	}
}
RAJ_support_INO = {
    allowed = {
       original_tag = RAJ 
    }
    enable = {
        INO = {
			has_war_with = WPA
		}
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = support
        id = INO
        value = 100
    }
}
HOL_support_WPA = {
    allowed = {
       original_tag = WPA
    }
    enable = {
        INO = {
			has_war_with = WPA
		}
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = support
        id = WPA
        value = 100
    }
}