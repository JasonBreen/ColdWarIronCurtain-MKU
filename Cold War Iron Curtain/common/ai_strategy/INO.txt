INO_Maluku = {
	allowed = { tag = INO }
	enable = { 
		INO = {
            has_war_with = SMK
        }
	}
	abort = { always = no }
	abort_when_not_enabled = yes
	
	ai_strategy = {
        type = invade
        id = SMK
        value = 10000
    }
}
RAJ_support_INO = {
    allowed = {
       original_tag = RAJ 
    }
    enable = {
        INO = {
			has_war_with = WPA
		}
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = send_lend_lease_desire
        id = INO
        value = 10000
    }
}
HOL_support_WPA = {
    allowed = {
       original_tag = WPA
    }
    enable = {
        INO = {
			has_war_with = WPA
		}
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = send_lend_lease_desire
        id = WPA
        value = 100
    }
}