####################################################################
# CZE (Czechoslovakia) AI strategy - Cold War 1949-1971
####################################################################

# Warsaw Pact member: stay wary of West Germany as long as the USSR backs it
# and CZE is not (somehow) allied to WGR. Guarded with country_exists so the
# prepare_for_war target is always a live country.
CZE_wary_of_WGR = {
	allowed = { tag = CZE }
	enable = {
		country_exists = SOV
		country_exists = WGR
		NOT = { is_ally_with = WGR }
	}
	abort_when_not_enabled = yes
	ai_strategy = {
		type = prepare_for_war
		id = WGR
		value = 120
	}
}
