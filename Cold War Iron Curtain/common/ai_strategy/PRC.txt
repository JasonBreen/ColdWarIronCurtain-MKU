# befriend
# conquer
# antagonize
# build_ship
# build_army
PRC_build_peacetime_army = {
	allowed = { tag = PRC }
	enable = { 
		date > 1949.1.1
		has_war = no
		has_army_manpower = { size < 1200000 }
	}
	abort_when_not_enabled = yes
	abort = { always = no }
	ai_strategy = { type = build_army id = motorized value = 500 }
	ai_strategy = { type = build_army id = infantry value = 400 }
	ai_strategy = { type = force_build_armies value = 300 }
}
PRC_build_wartime_army = {
	allowed = { tag = PRC }
	enable = { 
		date > 1949.1.1
		has_war = yes
		has_army_manpower = { size < 5000000 }
	}
	abort_when_not_enabled = yes
	abort = { always = no }
	ai_strategy = { type = build_army id = motorized value = 500 }
	ai_strategy = { type = build_army id = infantry value = 400 }
	ai_strategy = { type = force_build_armies value = 300 }
}
PRC_sino_soviet_pact = {
	allowed = { tag = PRC }
	enable = { 
		date < 1955.1.1
	}
	abort_when_not_enabled = yes
	abort = { always = no }
	ai_strategy = {
		type = front_unit_request
		tag = SOV
		tag = MON
		value = -200
	}
}
PRC_yunnan = {
	allowed = { tag = PRC }
	enable = { 
		country_exists = YAN
	}
	abort_when_not_enabled = yes
	abort = { always = no }
	ai_strategy = {
		type = front_unit_request
		tag = YAN
		value = 200
	}
}
PRC_tibet = {
	allowed = { tag = PRC }
	enable = { 
		country_exists = TIB
	}
	abort_when_not_enabled = yes
	abort = { always = no }
	ai_strategy = {
		type = front_unit_request
		tag = TIB
		tag = NAN
		tag = LIN
		tag = HOR
		tag = DER
		tag = CHK
		value = 200
	}
}

PRC_sino_soviet_pact = {
	allowed = { tag = PRC }
	enable = { 
		NOT = { KPA = { has_war_with = KOR } }
	}
	abort_when_not_enabled = yes
	abort = { always = no }
	ai_strategy = {
		type = front_unit_request
		tag = KPA
		value = -200
	}
}
PRC_supports_north = {
	allowed = { tag = PRC }
	enable = {
		KPA = { has_war_with = KOR }
	}
	abort_when_not_enabled = yes
	ai_strategy = {
		type = front_armor_score
		id = KOR
		value = 100
	}
	ai_strategy = {
		type = send_volunteers_desire
		id = "KPA"			
		value = -10000
	}
	ai_strategy = {
		type = force_defend_ally_borders
		value = 100
		target = asia
	}
	ai_strategy = {
		type = put_unit_buffers
		#you can define a target in following ways. you can define more than one

		order_id = 2
		states = { 
			1131
			920
			525
		}
		
		ratio = 0.25
		area = asia
		
		subtract_invasions_from_need = no
	}
}

PRC_Liberation_War_phase_one = {
    allowed = { original_tag = PRC }
    enable = { 
		has_global_flag = CCW_Started
		NOT = { controls_state = 1994 }
	}
    abort_when_not_enabled = yes
	ai_strategy = {
		type = front_armor_score
		id = CHI
		value = 100
	}
	ai_strategy = {
		type = front_control
		state = 1786
		strategic_region = 200				# Target a strategic region. Can specify multiple.
		strategic_region = 145				# Target a strategic region. Can specify multiple.
		ratio = 0.60			# At least this ratio of the frontline provinces need to be targeted for the AI strat to apply
		priority = 100			# Higher prio strats will override lower
		ordertype = front		# Can be either {front, invasion}
		execution_type = rush	# Can be either {careful, balanced, rush, rush_weak}
		execute_order = yes		# If set, will force activate/deactivate execution
		manual_attack = yes		# If set the AI will try manual poke attacks (for front orders)
	}
	ai_strategy = {
		type = front_control
		strategic_region = 144				# Target a strategic region. Can specify multiple.
		ratio = 0.2			# At least this ratio of the frontline provinces need to be targeted for the AI strat to apply
		priority = 20			# Higher prio strats will override lower
		ordertype = front		# Can be either {front, invasion}
		execution_type = careful	# Can be either {careful, balanced, rush, rush_weak}
		execute_order = yes		# If set, will force activate/deactivate execution
	}
	
	ai_strategy = {
		type = front_control
		strategic_region = 323				# Target a strategic region. Can specify multiple.
		strategic_region = 165				# Target a strategic region. Can specify multiple.
		strategic_region = 322				# Target a strategic region. Can specify multiple.
		ratio = 0.50			# At least this ratio of the frontline provinces need to be targeted for the AI strat to apply
		priority = 100			# Higher prio strats will override lower
		ordertype = front		# Can be either {front, invasion}
		execution_type = rush	# Can be either {careful, balanced, rush, rush_weak}
		execute_order = yes		# If set, will force activate/deactivate execution
		manual_attack = yes		# If set the AI will try manual poke attacks (for front orders)
	}
	ai_strategy = {
		type = garrison
		value = -1000
	}
}
PRC_Liberation_War_Final = {
    allowed = { original_tag = PRC }
    enable = { 
		has_global_flag = CCW_Started
		controls_state = 1994
	}
    abort_when_not_enabled = yes
	ai_strategy = {
		type = front_armor_score
		id = CHI
		value = 100
	}
	ai_strategy = {
		type = front_control
		strategic_region = 324				# Target a strategic region. Can specify multiple.
		strategic_region = 165				# Target a strategic region. Can specify multiple.
		strategic_region = 144				# Target a strategic region. Can specify multiple.
		ratio = 0.80			# At least this ratio of the frontline provinces need to be targeted for the AI strat to apply
		priority = 100			# Higher prio strats will override lower
		ordertype = front		# Can be either {front, invasion}
		execution_type = rush	# Can be either {careful, balanced, rush, rush_weak}
		execute_order = yes		# If set, will force activate/deactivate execution
		manual_attack = yes		# If set the AI will try manual poke attacks (for front orders)
	}
	ai_strategy = {
		type = garrison
		value = -1000
	}
}