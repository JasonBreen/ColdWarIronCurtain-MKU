FRA_build_peacetime_army = {
	allowed = { tag = FRA }
	enable = { 
		date > 1949.1.1
		has_war = no
		has_army_manpower = { size < 300000 }
	}
	abort_when_not_enabled = yes
	abort = { always = no }
	ai_strategy = { type = build_army id = motorized value = 500 }
	ai_strategy = { type = build_army id = infantry value = 400 }
	ai_strategy = { type = force_build_armies value = 300 }
}
FRA_build_wartime_army = {
	allowed = { tag = FRA }
	enable = { 
		date > 1949.1.1
		has_war = yes
		has_army_manpower = { size < 2000000 }
	}
	abort_when_not_enabled = yes
	abort = { always = no }
	ai_strategy = { type = build_army id = motorized value = 500 }
	ai_strategy = { type = build_army id = infantry value = 400 }
	ai_strategy = { type = force_build_armies value = 300 }
}
FRA_ignore_Italy = {
	allowed = { tag = FRA }
	enable = {
		NOT = { 
			OR = {
				has_war_with = ITA
			}
		}
	}
	abort = {
		OR = {
			has_war_with = ITA
		}
	}
	
	ai_strategy = {
		type = ignore
		id = "ITA"			
		value = 1000
	}


}
FRA_ignore_Swiss = {
	allowed = { tag = FRA }
	enable = {
		NOT = { 
			OR = {
				has_war_with = SWI
			}
		}
	}
	abort = {
		OR = {
			has_war_with = SWI
		}
	}
	
	ai_strategy = {
		type = ignore
		id = "SWI"			
		value = 1000
	}


}
FRA_ignore_Germany = {
	allowed = { OR = { tag = FRA tag = ENG } }
	enable = {
		NOT = { 
			has_war_with = WGR
		}
	}
	abort = {
		has_war_with = WGR
	}
	
	ai_strategy = {
		type = force_defend_ally_borders
		id = "WGR"			
		value = 100
	}
}
FRA_ignore_USA = {
	allowed = { OR = { tag = FRA tag = ENG } }
	enable = {
		NOT = { 
			has_war_with = USA
		}
	}
	abort = {
		has_war_with = USA
	}
	
	ai_strategy = {
		type = ignore
		id = "USA"			
		value = 1000
	}
}
FRE_Protect_Hanoi = {
	allowed = {
		tag = FRE
	}
	enable = {
		has_war_with = VIN
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_unit_request
		state = 1760
		value = 300
	}
}
FRE_Protect_vietnam = {
	allowed = {
		tag = FRE
	}
	enable = {
		has_war_with = VIN
	}
	abort_when_not_enabled = yes
	abort = { always = no }
	ai_strategy = {
		type = theatre_distribution_demand_increase
		id = 786
		value = 20
	}
}
FRE_defend_cao_bac = {
	allowed = {
		tag = FRE
	}
	enable = {
		has_war_with = VIN
		country_exists = THO
		VIN = { has_country_flag = VIN_Campaign_Cao_Bac_Active }
	}
	abort_when_not_enabled = yes
	abort = { always = no }

	ai_strategy = {
		type = front_control
		tag = VIN
		ordertype = front
		execution_type = careful
		priority = 300
	}
	ai_strategy = {
		type = front_unit_request
		state = 1280
		value = 300
	}
	ai_strategy = {
		type = front_unit_request
		state = 1768
		value = 300
	}
}
FRE_defend_tay_bac = {
	allowed = { tag = FRE }
	enable = {
		has_war_with = VIN
		VIN = { has_country_flag = VIN_Campaign_Tay_Bac_Active }
	}
	abort_when_not_enabled = yes
	abort = { always = no }
	ai_strategy = {
		type = front_control
		tag = VIN
		ordertype = front
		execution_type = careful
		priority = 300
	}
	ai_strategy = {
		type = front_unit_request
		state = 1761
		value = 275
	}
	ai_strategy = {
		type = front_unit_request
		state = 671
		value = 275
	}
}
FRE_defend_hoa_binh = {
	allowed = { tag = FRE }
	enable = {
		has_war_with = VIN
		VIN = { has_country_flag = VIN_Campaign_Hoa_Binh_Active }
	}
	abort_when_not_enabled = yes
	abort = { always = no }
	ai_strategy = {
		type = front_control
		tag = VIN
		ordertype = front
		execution_type = careful
		priority = 300
	}
	ai_strategy = {
		type = front_unit_request
		state = 1766
		value = 275
	}
	ai_strategy = {
		type = front_unit_request
		state = 671
		value = 250
	}
}
FRE_defend_dien_bien = {
	allowed = { tag = FRE }
	enable = {
		has_war_with = VIN
		VIN = { has_country_flag = VIN_Campaign_Dien_Bien_Active }
	}
	abort_when_not_enabled = yes
	abort = { always = no }
	ai_strategy = {
		type = front_control
		tag = VIN
		ordertype = front
		execution_type = careful
		priority = 350
	}
	ai_strategy = {
		type = front_unit_request
		state = 671
		value = 300
	}
}

# Crown-domain armies own the ground the corps is being asked to defend. Give
# their AI the same positional priorities instead of assuming FRE can directly
# move every subject formation.
THO_hold_cao_bac = {
	allowed = { tag = THO }
	enable = { has_war_with = VIN }
	abort_when_not_enabled = yes
	abort = { NOT = { has_war_with = VIN } }
	ai_strategy = {
		type = front_control
		tag = VIN
		ordertype = front
		execution_type = careful
		priority = 600
	}
	ai_strategy = { type = front_unit_request state = 1280 value = 300 }
	ai_strategy = { type = front_unit_request state = 1768 value = 300 }
}

TAI_hold_the_highlands = {
	allowed = { tag = TAI }
	enable = { has_war_with = VIN }
	abort_when_not_enabled = yes
	abort = { NOT = { has_war_with = VIN } }
	ai_strategy = {
		type = front_control
		tag = VIN
		ordertype = front
		execution_type = careful
		priority = 600
	}
	ai_strategy = { type = theatre_distribution_demand_increase id = 1761 value = 20 }
	ai_strategy = { type = theatre_distribution_demand_increase id = 671 value = 20 }
	ai_strategy = { type = front_unit_request state = 1761 value = 350 }
	ai_strategy = { type = front_unit_request state = 671 value = 350 }
}

TAM_hold_hoa_binh = {
	allowed = { tag = TAM }
	enable = { has_war_with = VIN }
	abort_when_not_enabled = yes
	abort = { NOT = { has_war_with = VIN } }
	ai_strategy = {
		type = front_control
		tag = VIN
		ordertype = front
		execution_type = careful
		priority = 600
	}
	ai_strategy = { type = front_unit_request state = 1766 value = 400 }
}
FRE_unit_buffer_for_Indochina = {
	allowed = {
		tag = FRE
	}
	enable = {
		has_war_with = VIN
	}
	abort = { always = no }
	abort_when_not_enabled = yes
	
	ai_strategy = {
		type = put_unit_buffers
		
		order_id = 2
		
		# Near Hanoi
		states = { 
			786
			1281
		}
		
		ratio = 0.2
		area = asia
		
		subtract_invasions_from_need = yes
	}
}

indochina_area_priority = {
	allowed = { tag = FRE }
	enable = {
		has_war_with = VIN
	}
	abort = { always = no }
	ai_strategy = {
		type = area_priority
		id = indochina_conflict
		value = 100
	}
}
LOS_build_wartime_army = {
	allowed = { tag = LOS }
	enable = { 
		date > 1949.1.1
		has_war = yes
		has_army_manpower = { size < 1000000 }
	}
	abort_when_not_enabled = yes
	abort = { always = no }
	ai_strategy = { type = build_army id = motorized value = 500 }
	ai_strategy = { type = build_army id = infantry value = 400 }
	ai_strategy = { type = force_build_armies value = 300 }
}
