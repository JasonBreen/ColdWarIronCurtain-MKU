# Intentional override stub.
# Overrides the vanilla common/ai_strategy/SLO.txt (which throws errors in CWIC:
#   unknown trigger-type 'going_war_with_SOV').
# SLO rarely appears in-game, so it falls back to generic AI. Left intentionally empty.
