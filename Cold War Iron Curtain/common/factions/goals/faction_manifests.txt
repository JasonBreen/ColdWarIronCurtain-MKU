@manifest_fulfilled_value = 0.75

###USSR###
faction_manifest_workers_of_the_world = {
	name = workers_of_the_world_goal_name
	description = workers_of_the_world_goal_desc
	is_manifest = yes
	ratio_progress = {
		total_amount_collection = game:all_states
		completed_amount_collection = collection:world_communist_states
		
		scale = {
		}
		
		progress_sections = {
			manifest_fulfilled = {
				min = @manifest_fulfilled_value
				modifier = { 
					required_garrison_factor = -0.15
					generate_wargoal_tension = -0.15
				}
			}
		}
	}
}
###NATO###
faction_manifest_animus_in_consulendo_liber = {
	name = animus_in_consulendo_liber_goal_name
	description = animus_in_consulendo_liber_goal_desc
	is_manifest = yes
	ratio_progress = {
		total_amount_collection = game:all_states
		completed_amount_collection = collection:world_non_communist_states
		
		scale = {
		}
		
		progress_sections = {
			manifest_fulfilled = {
				min = @manifest_fulfilled_value
				modifier = { 
					required_garrison_factor = -0.15
					generate_wargoal_tension = -0.15
					communism_drift = -0.20
					
				}
			}
		}
	}
}
#KAY
faction_manifest_KAY_give_me_liberty_or_give_me_death = {
	name = KAY_give_me_liberty_or_give_me_death_goal_name
	description = KAY_give_me_liberty_or_give_me_death_goal_desc
	is_manifest = yes
	ratio_progress = {
		total_amount_collection = game:all_states
		completed_amount_collection = collection:world_non_communist_states #go fucking kill yourself paradox interactive i hate all of you
		
		scale = {
			
		}
		
		progress_sections = {
			manifest_fulfilled = {
				min = 0
				modifier = { 
					army_core_defence_factor = 0.05
					attrition = -0.025
				}
			}
		}
	}
}
#ARA
faction_manifest_ARA_liberty_equality_fraternity = {
	name = ARA_liberty_equality_fraternity_goal_name
	description = ARA_liberty_equality_fraternity_goal_desc
	is_manifest = yes
	ratio_progress = {
		total_amount_collection = game:all_states
		completed_amount_collection = collection:world_non_communist_states
		
		scale = {
			
		}
		
		progress_sections = {
			manifest_fulfilled = {
				min = 0
				modifier = { 
					targeted_modifier = {
						tag = BRM
						defense_bonus_against = 0.025
						attack_bonus_against = 0.025
					}
					production_speed_buildings_factor = 0.05
				}
			}
		}
	}
}
#VIE

faction_manifest_VIE_Dan_viqui = {
	name = VIE_Dan_viqui_goal_name
	description = VIE_Dan_viqui_goal_desc
	is_manifest = yes
	ratio_progress = {
		total_amount_collection = game:all_states
		completed_amount_collection = collection:world_non_communist_states
		
		scale = {
			
		}
		
		progress_sections = {
			manifest_fulfilled = {
				min = 0
				army_speed_factor = 0.05
				production_factory_efficiency_gain_factor = 0.05
			}
		}
	}
}

#VIN

faction_manifest_VIN_Nothing_is_more_precious_than_independence_and_freedom = {
	name = VIN_Nothing_is_more_precious_than_independence_and_freedom_goal_name
	description = VIN_Nothing_is_more_precious_than_independence_and_freedom_desc
	is_manifest = yes
	ratio_progress = {
		total_amount_collection = game:all_states
		completed_amount_collection = collection:world_non_communist_states
		
		scale = {
			
		}
		
		progress_sections = {
			manifest_fulfilled = {
				min = 0
				modifier = { 
					targeted_modifier = {
						tag = FRA
						defense_bonus_against = 0.05
						attack_bonus_against = 0.05
					}
				}
			}
		}
	}
}