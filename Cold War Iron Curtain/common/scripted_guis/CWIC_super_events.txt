scripted_gui = {

	# Shared super event popup (normal / global events)
	CWIC_Super_Event_Window = {
		context_type = player_context
		window_name = "CWIC_Super_Event_Window"

		visible = {
			has_country_flag = CWIC_Super_Event_Visible
		}

		properties = {
			Super_Event_Image = {
				image = "[get_superevent_texture]"
			}
		}

		effects = {
			CWIC_Super_Event_Close_click = {
				CWIC_Clear_All_SE_Flags = yes
			}
		}
	}

	# Korea country-specific super event window (its own frame, shared dynamic picture)
	KOR_Big_Events_window = {
		context_type = player_context
		window_name = KOR_Big_Event_main_window

		visible = {
			has_country_flag = KOR_Big_Event_Flag
		}

		properties = {
			KOR_Big_Event_Picture = {
				image = "[get_superevent_texture]"
			}
		}

		effects = {
			KOR_Big_Event_Close_Button_click = {
				CWIC_Clear_All_SE_Flags = yes
			}
		}
	}

	# Japan country-specific super event window (its own frame, shared dynamic picture)
	JAP_Big_Events_window = {
		context_type = player_context
		window_name = JAP_Big_Event_main_window

		visible = {
			has_country_flag = JAP_Big_Event_Flag
		}

		properties = {
			JAP_Big_Event_Picture = {
				image = "[get_superevent_texture]"
			}
		}

		effects = {
			JAP_Big_Event_Close_Button_click = {
				CWIC_Clear_All_SE_Flags = yes
			}
		}
	}
}
