scripted_gui = {
  COL_PSG_GUI = {
    window_name = "COL_PSG_GUI_Window"
    context_type = player_context

    visible = {
		has_country_flag = open_COL_PSD_GUI
    }   #可視化

    effects = {
        Bottons_Close_click = {
            clr_country_flag = open_COL_PSD_GUI
	}
        }
    }   #ボタンによる効果

    triggers = {
    }   #ボタンを可視化をするトリガー、

    dynamic_lists = {
	}

    properties = {
    }   #国旗を表示させる

    ai_enabled = {
		always = no
    }

    ai_test_interval = 290  # The higher, the long it takes the ai to do actions
	ai_test_variance = 0.5

    ai_check = {
    }

    ai_check_scope = {
    }

    ai_weights = {
    }
    parent_window_token = {

    }   #他のGUIと同期させる
  }
}
