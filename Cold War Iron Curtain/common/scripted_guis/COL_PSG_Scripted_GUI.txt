scripted_gui = {
	COL_PSG_GUI = {
		context_type = player_context
		parent_window_token = politics_tab
		window_name = "COL_GUI_open_button"
		ai_enabled = { always = no }
		visible = {
			is_ai = no
			tag = COL
		}
		effects = {
			COL_GUI_open_button_click = {
				if = {
					limit = { has_country_flag = COL_PSD_GUI_FLAG }
					clr_country_flag = COL_PSD_GUI_FLAG
				}
				else = {
					set_country_flag = COL_PSD_GUI_FLAG
				}
			}
		}
	}
}
