scripted_gui = {
	
	COL_PSG_GUI_Window_opener = {
		context_type = player_context
		parent_window_token = politics_tab
		window_name = COL_PSG_GUI_Window_open
		
        ai_enabled = { always = no 
        }
		
        visible = {
			is_ai = no
		}

		effects = {
			COL_GUI_Open_Button_click = {
				set_country_flag = COL_PSG_GUI
			}
		}
	}
}