scripted_gui = {
	food_gui = {
		context_type = player_context
		parent_window_token = top_bar
		window_name = "food"
		dirty = refresh_topbar
		visible = {
			is_ai = no
		}

		triggers = {
		}

		effects = {
		}
	}
}
