Kamlon_Rebellion_Decisions = {
    Invade_Sulu = {
        ai_will_do = {
            factor = 1
        }
        visible = {
            country_exists = SLU
        }
        cost = 50
        days_remove = 30
        fire_only_once = yes
        complete_effect = {
			
        }
		remove_effect = {
			declare_war_on = {
				target = SLU
				type = annex_everything
			}
			custom_effect_tooltip = PHI_Invade_Sulu_tt
		}
    }
}
PHI_Development_Decisions = {
    PHI_Construct_Rural_Water_Infrastructure = {
		icon = GFX_decision_water_infrastructure
		fire_only_once = yes
		state_target = yes
		target_root_trigger = {
			has_completed_focus = PHI_Organize_Irrigation_Rural_Infrastructure_Construction
		}
		target_array = PHI.core_states
		target_trigger = {
			FROM = {
				is_owned_by = ROOT
			}
		}
		on_map_mode = map_only
		days_remove = 100
		cost = 0
		custom_cost_trigger = {
			check_variable = {
				currentMoney > 2
			}
		}
		custom_cost_text = PHI_has_2_bil
		complete_effect = {
			add_to_variable = {
				currentMoney = -2
			}
			set_temp_variable = {
				x = -2
				tooltip = adjust_money_mil2_tt
			}
		}
		remove_effect = {
			FROM = {
				add_building_construction = {
					type = water_infrastructure
					level = 1
					instant_build = yes
				}
			}
		}	
	}
	PHI_Expand_Rural_Settlements_Decision = {
		icon = GFX_decision_water_infrastructure
		fire_only_once = yes
		state_target = yes
		target_root_trigger = {
			has_completed_focus = PHI_Organize_Irrigation_Rural_Infrastructure_Construction
		}
		target_array = PHI.core_states
		target_trigger = {
			FROM = {
				is_owned_by = ROOT
				any_neighbour_state = {
					state = 327
				}
			}
		}
		on_map_mode = map_only
		days_remove = 100
		cost = 0
		custom_cost_trigger = {
			check_variable = {
				currentMoney > 0.5
			}
		}
		custom_cost_text = PHI_has_500_mil
		complete_effect = {
			add_to_variable = {
				currentMoney = -0.5
			}
			set_temp_variable = {
				x = -500
				tooltip = adjust_money_mil_tt
			}
		}
		remove_effect = {
			FROM = {
				add_extra_state_shared_building_slots = 2
			}
		}	
	}
}
