space_missions = {
	launch_mission = {
		icon = GFX_earth
		available = {
			NOT = { has_country_flag = ongoing_flight }
			amount_taken_ideas = {
				amount = 3 
				slots = {
					mission
					booster
					upper_stages
				}
			}
			check_variable = {
				modifier@space_mission_weight = modifier@space_mission_lift_capacity
				compare = less_than_or_equals
				tooltip = payload_req_tt
			}
			check_variable = { 
				modifier@space_mission_cost = currentbudget
				compare = less_than_or_equals
				tooltip = cost_req_tt
			}
			check_variable = { 
				modifier@space_mission_booster_reliability < 100
				
				tooltip = risk_booster_req_tt
			}
			check_variable = { 
				modifier@space_mission_upper_stage_reliability < 100
				
				tooltip = risk_upper_stage_req_tt
			}
			check_variable = { 
				modifier@space_mission_payload_reliability < 100
				
				tooltip = risk_payload_req_tt
			}
		}
		cost = 150
		days_re_enable = 10
		fire_only_once = no
		visible = {
			NOT = { has_country_flag = finished_unmanned_object_in_orbit }
		}
		complete_effect = {
			calculate_mission_timers = yes
			activate_mission = SPACE_MISSION_part1
			set_country_flag = space_mission_ongoing_part1
			set_country_flag = ongoing_flight
		}
		ai_will_do = {
			base = 0
			modifier = { add = 10 date > 1955.1.1 }
		}
	}
	SPACE_MISSION_part1 = {
		priority = 10
		icon = generic_ignite_civil_war
        days_mission_timeout = modifier@space_mission_building_length
		fire_only_once = no
		activation = {
			has_country_flag = space_mission_ongoing_part1
		}
		# allowed = {
			# has_country_flag = space_mission_ongoing_part1
		# }
		fixed_random_seed = no
		available = {
			always = no
		}
		is_good = yes
		timeout_effect = {
			random_list = {
				inv_up_rel = {
					activate_mission = SPACE_MISSION_part2
					set_country_flag = space_mission_ongoing_part2
				}
				up_rel = {
					clr_country_flag = ongoing_flight
				}
			}
			clr_country_flag = space_mission_ongoing_part1
		}
	}
	SPACE_MISSION_part2 = {
		priority = 10
		icon = generic_ignite_civil_war
        days_mission_timeout = 30
		fire_only_once = no
		activation = {
			has_country_flag = space_mission_ongoing_part2
		}
		# allowed = {
			# has_country_flag = space_mission_ongoing_part2
		# }
		available = {
			always = no
		}
		is_good = yes
		timeout_effect = {
			random_list = {
				inv_bo_rel = {
					activate_mission = SPACE_MISSION_part3
					set_country_flag = space_mission_ongoing_part3
				}
				bo_rel = {
					clr_country_flag = ongoing_flight
				}
			}
			clr_country_flag = space_mission_ongoing_part2
		}
	}
	SPACE_MISSION_part3 = {
		priority = 10
		icon = generic_ignite_civil_war
        days_mission_timeout = modifier@space_mission_mission_length
		fire_only_once = no
		activation = {
			has_country_flag = space_mission_ongoing_part3
		}
		# allowed = {
			# has_country_flag = space_mission_ongoing_part3
		# }
		available = {
			always = no
		}
		is_good = yes
		timeout_effect = {
			random_list = {
				inv_pa_rel = {
					add_stability = 0.5
				}
				pa_rel = {
					add_manpower = 10000
				}
			}
			clr_country_flag = space_mission_ongoing_part3
			clr_country_flag = ongoing_flight
		}
	}
}