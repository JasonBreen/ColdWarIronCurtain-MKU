
# ------------------------------------------------------------
# DECISIONS
# ------------------------------------------------------------

cwic_mnc_nationalization_targeted_cat = {
	cwic_mnc_nat_exxon_hydrocarbons = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_HYDRO
		available = {
			has_variable = corp_exxon_presence_hydrocarbons
			check_variable = { corp_exxon_presence_hydrocarbons > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_exxon_hydrocarbons }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_exxon_presence_hydrocarbons
			check_variable = { CWIC_mnc_presence_tier_hydrocarbons > 0 }
		}
		complete_effect = {
			cwic_mnc_nationalize_exxon_hydrocarbons = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_mobil_hydrocarbons = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_HYDRO
		available = {
			has_variable = corp_mobil_presence_hydrocarbons
			check_variable = { corp_mobil_presence_hydrocarbons > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_mobil_hydrocarbons }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_mobil_presence_hydrocarbons
			check_variable = { CWIC_mnc_presence_tier_hydrocarbons > 0 }
		}
		complete_effect = {
			cwic_mnc_nationalize_mobil_hydrocarbons = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_chevron_hydrocarbons = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_HYDRO
		available = {
			has_variable = corp_chevron_presence_hydrocarbons
			check_variable = { corp_chevron_presence_hydrocarbons > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_chevron_hydrocarbons }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_chevron_presence_hydrocarbons
			check_variable = { CWIC_mnc_presence_tier_hydrocarbons > 0 }
		}
		complete_effect = {
			cwic_mnc_nationalize_chevron_hydrocarbons = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_texaco_hydrocarbons = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_HYDRO
		available = {
			has_variable = corp_texaco_presence_hydrocarbons
			check_variable = { corp_texaco_presence_hydrocarbons > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_texaco_hydrocarbons }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_texaco_presence_hydrocarbons
			check_variable = { CWIC_mnc_presence_tier_hydrocarbons > 0 }
		}
		complete_effect = {
			cwic_mnc_nationalize_texaco_hydrocarbons = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_gulf_oil_hydrocarbons = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_HYDRO
		available = {
			has_variable = corp_gulf_oil_presence_hydrocarbons
			check_variable = { corp_gulf_oil_presence_hydrocarbons > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_gulf_oil_hydrocarbons }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_gulf_oil_presence_hydrocarbons
			check_variable = { CWIC_mnc_presence_tier_hydrocarbons > 0 }
		}
		complete_effect = {
			cwic_mnc_nationalize_gulf_oil_hydrocarbons = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_amoco_hydrocarbons = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_HYDRO
		available = {
			has_variable = corp_amoco_presence_hydrocarbons
			check_variable = { corp_amoco_presence_hydrocarbons > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_amoco_hydrocarbons }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_amoco_presence_hydrocarbons
			check_variable = { CWIC_mnc_presence_tier_hydrocarbons > 0 }
		}
		complete_effect = {
			cwic_mnc_nationalize_amoco_hydrocarbons = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_conoco_hydrocarbons = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_HYDRO
		available = {
			has_variable = corp_conoco_presence_hydrocarbons
			check_variable = { corp_conoco_presence_hydrocarbons > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_conoco_hydrocarbons }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_conoco_presence_hydrocarbons
			check_variable = { CWIC_mnc_presence_tier_hydrocarbons > 0 }
		}
		complete_effect = {
			cwic_mnc_nationalize_conoco_hydrocarbons = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_sinclair_oil_hydrocarbons = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_HYDRO
		available = {
			has_variable = corp_sinclair_oil_presence_hydrocarbons
			check_variable = { corp_sinclair_oil_presence_hydrocarbons > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_sinclair_oil_hydrocarbons }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_sinclair_oil_presence_hydrocarbons
			check_variable = { CWIC_mnc_presence_tier_hydrocarbons > 0 }
		}
		complete_effect = {
			cwic_mnc_nationalize_sinclair_oil_hydrocarbons = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_shell_hydrocarbons = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_HYDRO
		available = {
			has_variable = corp_shell_presence_hydrocarbons
			check_variable = { corp_shell_presence_hydrocarbons > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_shell_hydrocarbons }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_shell_presence_hydrocarbons
			check_variable = { CWIC_mnc_presence_tier_hydrocarbons > 0 }
		}
		complete_effect = {
			cwic_mnc_nationalize_shell_hydrocarbons = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_bp_hydrocarbons = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_HYDRO
		available = {
			has_variable = corp_bp_presence_hydrocarbons
			check_variable = { corp_bp_presence_hydrocarbons > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_bp_hydrocarbons }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_bp_presence_hydrocarbons
			check_variable = { CWIC_mnc_presence_tier_hydrocarbons > 0 }
		}
		complete_effect = {
			cwic_mnc_nationalize_bp_hydrocarbons = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_total_cfp_hydrocarbons = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_HYDRO
		available = {
			has_variable = corp_total_cfp_presence_hydrocarbons
			check_variable = { corp_total_cfp_presence_hydrocarbons > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_total_cfp_hydrocarbons }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_total_cfp_presence_hydrocarbons
			check_variable = { CWIC_mnc_presence_tier_hydrocarbons > 0 }
		}
		complete_effect = {
			cwic_mnc_nationalize_total_cfp_hydrocarbons = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_eni_hydrocarbons = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_HYDRO
		available = {
			has_variable = corp_eni_presence_hydrocarbons
			check_variable = { corp_eni_presence_hydrocarbons > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_eni_hydrocarbons }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_eni_presence_hydrocarbons
			check_variable = { CWIC_mnc_presence_tier_hydrocarbons > 0 }
		}
		complete_effect = {
			cwic_mnc_nationalize_eni_hydrocarbons = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_iberia_cepsa_hydrocarbons = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_HYDRO
		available = {
			has_variable = corp_iberia_cepsa_presence_hydrocarbons
			check_variable = { corp_iberia_cepsa_presence_hydrocarbons > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_iberia_cepsa_hydrocarbons }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_iberia_cepsa_presence_hydrocarbons
			check_variable = { CWIC_mnc_presence_tier_hydrocarbons > 0 }
		}
		complete_effect = {
			cwic_mnc_nationalize_iberia_cepsa_hydrocarbons = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_statoil_hydrocarbons = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_HYDRO
		available = {
			has_variable = corp_statoil_presence_hydrocarbons
			check_variable = { corp_statoil_presence_hydrocarbons > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_statoil_hydrocarbons }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_statoil_presence_hydrocarbons
			check_variable = { CWIC_mnc_presence_tier_hydrocarbons > 0 }
		}
		complete_effect = {
			cwic_mnc_nationalize_statoil_hydrocarbons = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_aramco_hydrocarbons = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_HYDRO
		available = {
			has_variable = corp_aramco_presence_hydrocarbons
			check_variable = { corp_aramco_presence_hydrocarbons > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_aramco_hydrocarbons }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_aramco_presence_hydrocarbons
			check_variable = { CWIC_mnc_presence_tier_hydrocarbons > 0 }
		}
		complete_effect = {
			cwic_mnc_nationalize_aramco_hydrocarbons = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_pdvsa_hydrocarbons = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_HYDRO
		available = {
			has_variable = corp_pdvsa_presence_hydrocarbons
			check_variable = { corp_pdvsa_presence_hydrocarbons > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_pdvsa_hydrocarbons }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_pdvsa_presence_hydrocarbons
			check_variable = { CWIC_mnc_presence_tier_hydrocarbons > 0 }
		}
		complete_effect = {
			cwic_mnc_nationalize_pdvsa_hydrocarbons = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_nnpc_hydrocarbons = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_HYDRO
		available = {
			has_variable = corp_nnpc_presence_hydrocarbons
			check_variable = { corp_nnpc_presence_hydrocarbons > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_nnpc_hydrocarbons }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_nnpc_presence_hydrocarbons
			check_variable = { CWIC_mnc_presence_tier_hydrocarbons > 0 }
		}
		complete_effect = {
			cwic_mnc_nationalize_nnpc_hydrocarbons = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_sonatrach_hydrocarbons = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_HYDRO
		available = {
			has_variable = corp_sonatrach_presence_hydrocarbons
			check_variable = { corp_sonatrach_presence_hydrocarbons > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_sonatrach_hydrocarbons }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_sonatrach_presence_hydrocarbons
			check_variable = { CWIC_mnc_presence_tier_hydrocarbons > 0 }
		}
		complete_effect = {
			cwic_mnc_nationalize_sonatrach_hydrocarbons = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_pertamina_hydrocarbons = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_HYDRO
		available = {
			has_variable = corp_pertamina_presence_hydrocarbons
			check_variable = { corp_pertamina_presence_hydrocarbons > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_pertamina_hydrocarbons }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_pertamina_presence_hydrocarbons
			check_variable = { CWIC_mnc_presence_tier_hydrocarbons > 0 }
		}
		complete_effect = {
			cwic_mnc_nationalize_pertamina_hydrocarbons = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_pemex_hydrocarbons = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_HYDRO
		available = {
			has_variable = corp_pemex_presence_hydrocarbons
			check_variable = { corp_pemex_presence_hydrocarbons > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_pemex_hydrocarbons }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_pemex_presence_hydrocarbons
			check_variable = { CWIC_mnc_presence_tier_hydrocarbons > 0 }
		}
		complete_effect = {
			cwic_mnc_nationalize_pemex_hydrocarbons = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_rio_tinto_mining = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_MINING
		available = {
			has_variable = corp_rio_tinto_presence_mining
			check_variable = { corp_rio_tinto_presence_mining > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_rio_tinto_mining }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_rio_tinto_presence_mining
			check_variable = { CWIC_mnc_presence_tier_mining > 0 }
		}
		complete_effect = {
			cwic_mnc_nationalize_rio_tinto_mining = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_anglo_american_mining = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_MINING
		available = {
			has_variable = corp_anglo_american_presence_mining
			check_variable = { corp_anglo_american_presence_mining > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_anglo_american_mining }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_anglo_american_presence_mining
			check_variable = { CWIC_mnc_presence_tier_mining > 0 }
		}
		complete_effect = {
			cwic_mnc_nationalize_anglo_american_mining = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_de_beers_mining = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_MINING
		available = {
			has_variable = corp_de_beers_presence_mining
			check_variable = { corp_de_beers_presence_mining > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_de_beers_mining }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_de_beers_presence_mining
			check_variable = { CWIC_mnc_presence_tier_mining > 0 }
		}
		complete_effect = {
			cwic_mnc_nationalize_de_beers_mining = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_lonrho_mining = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_MINING
		available = {
			has_variable = corp_lonrho_presence_mining
			check_variable = { corp_lonrho_presence_mining > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_lonrho_mining }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_lonrho_presence_mining
			check_variable = { CWIC_mnc_presence_tier_mining > 0 }
		}
		complete_effect = {
			cwic_mnc_nationalize_lonrho_mining = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_inmetco_mining = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_MINING
		available = {
			has_variable = corp_inmetco_presence_mining
			check_variable = { corp_inmetco_presence_mining > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_inmetco_mining }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_inmetco_presence_mining
			check_variable = { CWIC_mnc_presence_tier_mining > 0 }
		}
		complete_effect = {
			cwic_mnc_nationalize_inmetco_mining = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_inco_mining = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_MINING
		available = {
			has_variable = corp_inco_presence_mining
			check_variable = { corp_inco_presence_mining > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_inco_mining }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_inco_presence_mining
			check_variable = { CWIC_mnc_presence_tier_mining > 0 }
		}
		complete_effect = {
			cwic_mnc_nationalize_inco_mining = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_alcoa_mining = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_MINING
		available = {
			has_variable = corp_alcoa_presence_mining
			check_variable = { corp_alcoa_presence_mining > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_alcoa_mining }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_alcoa_presence_mining
			check_variable = { CWIC_mnc_presence_tier_mining > 0 }
		}
		complete_effect = {
			cwic_mnc_nationalize_alcoa_mining = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_kennecott_mining = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_MINING
		available = {
			has_variable = corp_kennecott_presence_mining
			check_variable = { corp_kennecott_presence_mining > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_kennecott_mining }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_kennecott_presence_mining
			check_variable = { CWIC_mnc_presence_tier_mining > 0 }
		}
		complete_effect = {
			cwic_mnc_nationalize_kennecott_mining = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_anaconda_copper_mining = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_MINING
		available = {
			has_variable = corp_anaconda_copper_presence_mining
			check_variable = { corp_anaconda_copper_presence_mining > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_anaconda_copper_mining }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_anaconda_copper_presence_mining
			check_variable = { CWIC_mnc_presence_tier_mining > 0 }
		}
		complete_effect = {
			cwic_mnc_nationalize_anaconda_copper_mining = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_noranda_mining = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_MINING
		available = {
			has_variable = corp_noranda_presence_mining
			check_variable = { corp_noranda_presence_mining > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_noranda_mining }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_noranda_presence_mining
			check_variable = { CWIC_mnc_presence_tier_mining > 0 }
		}
		complete_effect = {
			cwic_mnc_nationalize_noranda_mining = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_sumitomo_mining_mining = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_MINING
		available = {
			has_variable = corp_sumitomo_mining_presence_mining
			check_variable = { corp_sumitomo_mining_presence_mining > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_sumitomo_mining_mining }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_sumitomo_mining_presence_mining
			check_variable = { CWIC_mnc_presence_tier_mining > 0 }
		}
		complete_effect = {
			cwic_mnc_nationalize_sumitomo_mining_mining = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_gécamines_mining = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_MINING
		available = {
			has_variable = corp_gécamines_presence_mining
			check_variable = { corp_gécamines_presence_mining > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_gécamines_mining }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_gécamines_presence_mining
			check_variable = { CWIC_mnc_presence_tier_mining > 0 }
		}
		complete_effect = {
			cwic_mnc_nationalize_gécamines_mining = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_united_fruit_agri = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_AGRI
		available = {
			has_variable = corp_united_fruit_presence_agri
			check_variable = { corp_united_fruit_presence_agri > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_united_fruit_agri }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_united_fruit_presence_agri
			check_variable = { CWIC_mnc_presence_tier_agri > 0 }
		}
		complete_effect = {
			cwic_mnc_nationalize_united_fruit_agri = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_del_monte_agri = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_AGRI
		available = {
			has_variable = corp_del_monte_presence_agri
			check_variable = { corp_del_monte_presence_agri > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_del_monte_agri }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_del_monte_presence_agri
			check_variable = { CWIC_mnc_presence_tier_agri > 0 }
		}
		complete_effect = {
			cwic_mnc_nationalize_del_monte_agri = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_dole_agri = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_AGRI
		available = {
			has_variable = corp_dole_presence_agri
			check_variable = { corp_dole_presence_agri > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_dole_agri }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			check_variable = { CWIC_mnc_presence_tier_agri > 0 }
		}
		complete_effect = {
			has_variable = corp_dole_presence_agri
			cwic_mnc_nationalize_dole_agri = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_cargill_agri = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_AGRI
		available = {
			has_variable = corp_cargill_presence_agri
			check_variable = { corp_cargill_presence_agri > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_cargill_agri }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_cargill_presence_agri
			check_variable = { CWIC_mnc_presence_tier_agri > 0 }
		}
		complete_effect = {
			cwic_mnc_nationalize_cargill_agri = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_adm_agri = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_AGRI
		available = {
			has_variable = corp_adm_presence_agri
			check_variable = { corp_adm_presence_agri > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_adm_agri }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_adm_presence_agri
			check_variable = { CWIC_mnc_presence_tier_agri > 0 }
		}
		complete_effect = {
			
			cwic_mnc_nationalize_adm_agri = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_bunge_agri = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_AGRI
		available = {
			has_variable = corp_bunge_presence_agri
			check_variable = { corp_bunge_presence_agri > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_bunge_agri }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_bunge_presence_agri
			check_variable = { CWIC_mnc_presence_tier_agri > 0 }
		}
		complete_effect = {
			cwic_mnc_nationalize_bunge_agri = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_unilever_agri = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_AGRI
		available = {
			has_variable = corp_unilever_presence_agri
			check_variable = { corp_unilever_presence_agri > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_unilever_agri }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_unilever_presence_agri
			check_variable = { CWIC_mnc_presence_tier_agri > 0 }
		}
		complete_effect = {
			cwic_mnc_nationalize_unilever_agri = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_nestle_agri = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_AGRI
		available = {
			has_variable = corp_nestle_presence_agri
			check_variable = { corp_nestle_presence_agri > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_nestle_agri }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_nestle_presence_agri
			check_variable = { CWIC_mnc_presence_tier_agri > 0 }
		}
		complete_effect = {
			cwic_mnc_nationalize_nestle_agri = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_cadbury_agri = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_AGRI
		available = {
			has_variable = corp_cadbury_presence_agri
			check_variable = { corp_cadbury_presence_agri > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_cadbury_agri }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_cadbury_presence_agri
			check_variable = { CWIC_mnc_presence_tier_agri > 0 }
		}
		complete_effect = {
			cwic_mnc_nationalize_cadbury_agri = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_mitsui_bussan_agri_agri = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_AGRI
		available = {
			has_variable = corp_mitsui_bussan_agri_presence_agri
			check_variable = { corp_mitsui_bussan_agri_presence_agri > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_mitsui_bussan_agri_agri }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_mitsui_bussan_agri_presence_agri
			check_variable = { CWIC_mnc_presence_tier_agri > 0 }
		}
		complete_effect = {
			cwic_mnc_nationalize_mitsui_bussan_agri_agri = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_gm_manufacturing = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_MFG
		available = {
			has_variable = corp_gm_presence_manufacturing
			check_variable = { corp_gm_presence_manufacturing > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_gm_manufacturing }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_gm_presence_manufacturing
			check_variable = { CWIC_mnc_presence_tier_manufacturing > 0 }
		}
		complete_effect = {
			cwic_mnc_nationalize_gm_manufacturing = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_ford_manufacturing = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_MFG
		available = {
			has_variable = corp_ford_presence_manufacturing
			check_variable = { corp_ford_presence_manufacturing > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_ford_manufacturing }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_ford_presence_manufacturing
			check_variable = { CWIC_mnc_presence_tier_manufacturing > 0 }
		}
		complete_effect = {
			cwic_mnc_nationalize_ford_manufacturing = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_chrysler_manufacturing = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_MFG
		available = {
			has_variable = corp_chrysler_presence_manufacturing
			check_variable = { corp_chrysler_presence_manufacturing > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_chrysler_manufacturing }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_chrysler_presence_manufacturing
			check_variable = { CWIC_mnc_presence_tier_manufacturing > 0 }
		}
		complete_effect = {
			cwic_mnc_nationalize_chrysler_manufacturing = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_vw_manufacturing = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_MFG
		available = {
			has_variable = corp_vw_presence_manufacturing
			check_variable = { corp_vw_presence_manufacturing > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_vw_manufacturing }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_vw_presence_manufacturing
			check_variable = { CWIC_mnc_presence_tier_manufacturing > 0 }
		}
		complete_effect = {
			cwic_mnc_nationalize_vw_manufacturing = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_fiat_manufacturing = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_MFG
		available = {
			has_variable = corp_fiat_presence_manufacturing
			check_variable = { corp_fiat_presence_manufacturing > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_fiat_manufacturing }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_fiat_presence_manufacturing
			check_variable = { CWIC_mnc_presence_tier_manufacturing > 0 }
		}
		complete_effect = {
			cwic_mnc_nationalize_fiat_manufacturing = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_renault_manufacturing = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_MFG
		available = {
			has_variable = corp_renault_presence_manufacturing
			check_variable = { corp_renault_presence_manufacturing > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_renault_manufacturing }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_renault_presence_manufacturing
			check_variable = { CWIC_mnc_presence_tier_manufacturing > 0 }
		}
		complete_effect = {
			cwic_mnc_nationalize_renault_manufacturing = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_peugeot_manufacturing = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_MFG
		available = {
			has_variable = corp_peugeot_presence_manufacturing
			check_variable = { corp_peugeot_presence_manufacturing > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_peugeot_manufacturing }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_peugeot_presence_manufacturing
			check_variable = { CWIC_mnc_presence_tier_manufacturing > 0 }
		}
		complete_effect = {
			cwic_mnc_nationalize_peugeot_manufacturing = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_daimler_benz_manufacturing = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_MFG
		available = {
			has_variable = corp_daimler_benz_presence_manufacturing
			check_variable = { corp_daimler_benz_presence_manufacturing > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_daimler_benz_manufacturing }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_daimler_benz_presence_manufacturing
			check_variable = { CWIC_mnc_presence_tier_manufacturing > 0 }
		}
		complete_effect = {
			cwic_mnc_nationalize_daimler_benz_manufacturing = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_toyota_manufacturing = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_MFG
		available = {
			has_variable = corp_toyota_presence_manufacturing
			check_variable = { corp_toyota_presence_manufacturing > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_toyota_manufacturing }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_toyota_presence_manufacturing
			check_variable = { CWIC_mnc_presence_tier_manufacturing > 0 }
		}
		complete_effect = {
			cwic_mnc_nationalize_toyota_manufacturing = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_nissan_manufacturing = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_MFG
		available = {
			has_variable = corp_nissan_presence_manufacturing
			check_variable = { corp_nissan_presence_manufacturing > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_nissan_manufacturing }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_nissan_presence_manufacturing
			check_variable = { CWIC_mnc_presence_tier_manufacturing > 0 }
		}
		complete_effect = {
			cwic_mnc_nationalize_nissan_manufacturing = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_ge_manufacturing = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_MFG
		available = {
			has_variable = corp_ge_presence_manufacturing
			check_variable = { corp_ge_presence_manufacturing > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_ge_manufacturing }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_ge_presence_manufacturing
			check_variable = { CWIC_mnc_presence_tier_manufacturing > 0 }
		}
		complete_effect = {
			cwic_mnc_nationalize_ge_manufacturing = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_dupont_manufacturing = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_MFG
		available = {
			has_variable = corp_dupont_presence_manufacturing
			check_variable = { corp_dupont_presence_manufacturing > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_dupont_manufacturing }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_dupont_presence_manufacturing
			check_variable = { CWIC_mnc_presence_tier_manufacturing > 0 }
		}
		complete_effect = {
			cwic_mnc_nationalize_dupont_manufacturing = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_dow_manufacturing = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_MFG
		available = {
			has_variable = corp_dow_presence_manufacturing
			check_variable = { corp_dow_presence_manufacturing > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_dow_manufacturing }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_dow_presence_manufacturing
			check_variable = { CWIC_mnc_presence_tier_manufacturing > 0 }
		}
		complete_effect = {
			cwic_mnc_nationalize_dow_manufacturing = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_basf_manufacturing = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_MFG
		available = {
			has_variable = corp_basf_presence_manufacturing
			check_variable = { corp_basf_presence_manufacturing > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_basf_manufacturing }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_basf_presence_manufacturing
			check_variable = { CWIC_mnc_presence_tier_manufacturing > 0 }
		}
		complete_effect = {
			cwic_mnc_nationalize_basf_manufacturing = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_bayer_manufacturing = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_MFG
		available = {
			has_variable = corp_bayer_presence_manufacturing
			check_variable = { corp_bayer_presence_manufacturing > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_bayer_manufacturing }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_bayer_presence_manufacturing
			check_variable = { CWIC_mnc_presence_tier_manufacturing > 0 }
		}
		complete_effect = {
			cwic_mnc_nationalize_bayer_manufacturing = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_ibm_electronics = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_ELEC
		available = {
			has_variable = corp_ibm_presence_electronics
			check_variable = { corp_ibm_presence_electronics > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_ibm_electronics }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_ibm_presence_electronics
			check_variable = { CWIC_mnc_presence_tier_electronics > 0 }
		}
		complete_effect = {
			cwic_mnc_nationalize_ibm_electronics = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_rca_electronics = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_ELEC
		available = {
			has_variable = corp_rca_presence_electronics
			check_variable = { corp_rca_presence_electronics > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_rca_electronics }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_rca_presence_electronics
			check_variable = { CWIC_mnc_presence_tier_electronics > 0 }
		}
		complete_effect = {
			cwic_mnc_nationalize_rca_electronics = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_att_electronics = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_ELEC
		available = {
			has_variable = corp_att_presence_electronics
			check_variable = { corp_att_presence_electronics > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_att_electronics }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_att_presence_electronics
			check_variable = { CWIC_mnc_presence_tier_electronics > 0 }
		}
		complete_effect = {
			cwic_mnc_nationalize_att_electronics = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_itt_electronics = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_ELEC
		available = {
			has_variable = corp_itt_presence_electronics
			check_variable = { corp_itt_presence_electronics > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_itt_electronics }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_itt_presence_electronics
			check_variable = { CWIC_mnc_presence_tier_electronics > 0 }
		}
		complete_effect = {
			cwic_mnc_nationalize_itt_electronics = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_siemens_electronics = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_ELEC
		available = {
			has_variable = corp_siemens_presence_electronics
			check_variable = { corp_siemens_presence_electronics > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_siemens_electronics }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_siemens_presence_electronics
			check_variable = { CWIC_mnc_presence_tier_electronics > 0 }
		}
		complete_effect = {
			cwic_mnc_nationalize_siemens_electronics = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_philips_electronics = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_ELEC
		available = {
			has_variable = corp_philips_presence_electronics
			check_variable = { corp_philips_presence_electronics > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_philips_electronics }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_philips_presence_electronics
			check_variable = { CWIC_mnc_presence_tier_electronics > 0 }
		}
		complete_effect = {
			cwic_mnc_nationalize_philips_electronics = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_olivetti_electronics = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_ELEC
		available = {
			has_variable = corp_olivetti_presence_electronics
			check_variable = { corp_olivetti_presence_electronics > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_olivetti_electronics }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_olivetti_presence_electronics
			check_variable = { CWIC_mnc_presence_tier_electronics > 0 }
		}
		complete_effect = {
			cwic_mnc_nationalize_olivetti_electronics = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_sony_electronics = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_ELEC
		available = {
			has_variable = corp_sony_presence_electronics
			check_variable = { corp_sony_presence_electronics > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_sony_electronics }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_sony_presence_electronics
			check_variable = { CWIC_mnc_presence_tier_electronics > 0 }
		}
		complete_effect = {
			cwic_mnc_nationalize_sony_electronics = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_panasonic_electronics = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_ELEC
		available = {
			has_variable = corp_panasonic_presence_electronics
			check_variable = { corp_panasonic_presence_electronics > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_panasonic_electronics }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_panasonic_presence_electronics
			check_variable = { CWIC_mnc_presence_tier_electronics > 0 }
		}
		complete_effect = {
			cwic_mnc_nationalize_panasonic_electronics = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

	cwic_mnc_nat_toshiba_electronics = {
		icon = generic_propaganda
		category = cwic_mnc_nationalization_targeted_cat
		cost = global.CWIC_MNC_NAT_PP_COST_ELEC
		available = {
			has_variable = corp_toshiba_presence_electronics
			check_variable = { corp_toshiba_presence_electronics > 0 }
			NOT = { has_country_flag = cwic_mnc_frozen_toshiba_electronics }
			OR = {
				check_variable = { CWIC_nationalization_pressure_index > 1 }
				check_variable = { CWIC_instability_index > 2 }
			}
		}
		visible = {
			has_variable = corp_toshiba_presence_electronics
			check_variable = { CWIC_mnc_presence_tier_electronics > 0 }
		}
		complete_effect = {
			cwic_mnc_nationalize_toshiba_electronics = yes
		}
		ai_will_do = {
			factor = 0.01
			modifier = { factor = 3 check_variable = { CWIC_nationalization_pressure_index > 3 } }
			modifier = { factor = 3 check_variable = { CWIC_instability_index > 2 } }
		}
	}

}
