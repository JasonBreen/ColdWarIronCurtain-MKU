DMC_Insurgency_Decisions = {
	constitutionalist_insurgency = {

		icon = generic_civil_support

		available = {		
			always = no
		}
		
		days_mission_timeout = 60
		activation = {
			has_country_flag = pro_bosch_insurgency
			check_variable = { pro_bosch_insurgency > 99 }
		}
		
		cancel_trigger = {
			check_variable = { pro_bosch_insurgency < 100 }
		}
		visible = {
		}
		is_good = no

		complete_effect = {
			release = DMC
			1159 = {
				clr_state_flag = pro_bosch_uprising
				clr_state_flag = insurg_lvl1
				remove_province_modifier = {
					static_modifiers = { insurgency_tier1 }
					province = {
						all_provinces = yes
					}
				}
				add_state_modifier = { modifier = { local_factory_sabotage = -0.05 } }
			}
		}
	}
	
	crush_caamano_rebels = {

		icon = generic_civil_support
		available = {
			has_country_flag = dom_uprising_insurgency
			check_variable = { dom_uprising_insurgency < 10 }
			custom_trigger_tooltip = { tooltip = insurgency_below_ten_TT }
		
			divisions_in_state = {
				size > 2
				state = 1159
			}
			
		}
		ai_will_do = {
			factor = 1
		}
		fire_only_once = yes
		
		cost = 100
		
		
		complete_effect = {
			clear_variable = constitutionalist_insurgency
			clr_country_flag = constitutionalist_insurgency

			1159 = {
				clr_state_flag = pro_bosch_uprising
				clr_state_flag = insurg_lvl1
				remove_province_modifier = {
					static_modifiers = { insurgency_tier1 }
					province = {
						all_provinces = yes
					}
				}
				add_state_modifier = { modifier = { local_factory_sabotage = -0.10 } }
			}
		}
	}
	
	Bomb_insurgents = {

		icon = generic_civil_support
		visible = {
			
		}
		available = {
			has_country_flag = pro_bosch_insurgency
			has_deployed_air_force_size = {
				size > 10
				type = cas
			}
		}
		ai_will_do = {
			factor = 1
		}
		fire_only_once = no
		days_re_enable = 90
		
		fixed_random_seed = no
		
		cost = 25
		
		complete_effect = {
			random_list = {
				75 = {
					country_event = DOM_insurgency_bombing.1
				}
				25 = {
					country_event = DOM_insurgency_bombing.0
				}
			}
		}
	}
}
