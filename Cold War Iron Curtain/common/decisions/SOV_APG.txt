sov_khrushchev_fate = {
	khrushchev_fate_mission = {		#ID of the mission, must not contain spaces!

		icon = GFX_decision_icon_default		
		days_mission_timeout = 50	#The amount of days remaining until the mission is removed.
		is_good = no		
		activation = { has_completed_focus = SOV_bulganin_union }	
		available = {		
			 NOT = { check_variable = { bulg_support < 5 } }
		}
		
		fire_only_once = yes

		timeout_effect = {		
		}
		
		complete_effect  = {
                complete_national_focus = SOV_pr_take_mea_again_cornman
		}
    }
	
    sov_arrest_serov = {
        icon = generic_purge
        cost = 20
        days_remove = 10
		fire_only_once = yes
        available = { check_variable = { var = doing_sum value = 1 compare = equals} }
        ai_will_do = { factor = 0 }

        complete_effect = {
            add_to_variable = { var = doing_sum value = -1 }
        }
		remove_effect = { country_event = { id = Bulg.101 } }
    }
	
    sov_arrest_furtseva = {
        icon = generic_purge
        cost = 20
        days_remove = 10
		fire_only_once = yes
        available = { check_variable = { var = doing_sum value = 1 compare = equals} check_variable = { var = Suslov_Assured value = 1 compare = equals} }
        ai_will_do = { factor = 0 }

        complete_effect = {
            add_to_variable = { var = doing_sum value = -1 }
        }
		remove_effect = { country_event = { id = Bulg.104 } }
    }	

    sov_assure_suslov = {
        icon = generic_political_reform
        cost = 20
        days_remove = 10
		fire_only_once = yes
        available = { check_variable = { var = doing_sum value = 1 } }
        ai_will_do = { factor = 0 }

        complete_effect = {
		    add_to_variable = { var = doing_sum value = -1 } 
			set_variable = { var = Suslov_Assured = 1 }
        }
		remove_effect = { country_event = { id = Bulg.102 } }
    }

    sov_sideline_zhukov = {
        icon = generic_purge
        cost = 20
        days_remove = 10
		fire_only_once = yes
        available = { check_variable = { var = doing_sum value = 1 } }
        ai_will_do = { factor = 0 }

        complete_effect = {
		    add_to_variable = { var = doing_sum value = -1 }
        }
		remove_effect = { country_event = { id = Bulg.103 } }
    }
}