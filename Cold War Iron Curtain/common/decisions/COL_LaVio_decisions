La_Violencia_Category = {
	La_Violencia = {

		icon = generic_industry

		available = {
			is_ai = no
		}

		allowed = {
			tag = COL
		}

		visible = {
			is_debug = yes
		}

		fire_only_once = no

		cost = 0

		complete_effect = {
			set_country_flag = COL_PSD_GUI_FLAG
		}
		
		ai_will_do = {
			factor = 0
		}
	}
}
