FRE_CEFEO_Missions_category = {
	# Resource barter decisions removed: supply now flows through the
	# Metropole lifeline requests in FRE_Metropole.txt.

	# Mission 1: Assess Route Coloniale 4
	FRE_Mission_Assess_RC4 = {
		icon = generic_intelligence_agency

		allowed = {
			tag = FRE
		}

		activation = {
			has_country_flag = FRE_Mission_Assess_RC4_Activated
			NOT = { has_country_flag = FRE_Mission_Assess_RC4_Completed }
		}

		available = {
			has_country_flag = FRE_RC4_Assessment_Complete
			NOT = { has_country_flag = FRE_Mission_Assess_RC4_Completed }
			NOT = { has_country_flag = FRE_Mission_Assess_RC4_Failed }
			custom_trigger_tooltip = {
				tooltip = FRE_Mission_Assess_RC4_Available_tt
				always = yes
			}
		}

		selectable_mission = yes
		days_mission_timeout = 30

		cost = 50

		fire_only_once = yes

		complete_effect = {
			fre_mission_calculate_rc4_success = yes
			# Calculate failure chance (100 - success chance)
			set_temp_variable = { mission_failure_chance = 100 }
			subtract_from_temp_variable = { mission_failure_chance = mission_success_chance }

			random_list = {
				# Dynamic success chance based on temp variable
				temp_var:mission_success_chance = {
					country_event = { id = FRE_Events.7 days = 1 }
				}
				# Failure chance is remainder
				temp_var:mission_failure_chance = {
					country_event = { id = FRE_Events.8 days = 1 }
				}
			}
		}

		timeout_effect = {
			# Timeout = failure
			country_event = { id = FRE_Events.8 days = 1 }
		}

		ai_will_do = {
			factor = 100
		}
	}

	# Mission 2: Address Revers Report Fallout
	FRE_Mission_Address_Revers_Report = {
		icon = generic_political_discourse

		allowed = {
			tag = FRE
		}

		activation = {
			has_country_flag = FRE_Mission_Address_Revers_Report_Activated
			NOT = { has_country_flag = FRE_Mission_Address_Revers_Report_Completed }
		}

		available = {
			has_country_flag = FRE_Revers_Report_Addressed
			NOT = { has_country_flag = FRE_Mission_Address_Revers_Report_Completed }
			NOT = { has_country_flag = FRE_Mission_Address_Revers_Report_Failed }
			custom_trigger_tooltip = {
				tooltip = FRE_Mission_Address_Revers_Report_Available_tt
				always = yes
			}
		}

		selectable_mission = yes
		days_mission_timeout = 45

		cost = 75

		fire_only_once = yes

		complete_effect = {
			fre_mission_calculate_revers_report_success = yes
			# Calculate failure chance (100 - success chance)
			set_temp_variable = { mission_failure_chance = 100 }
			subtract_from_temp_variable = { mission_failure_chance = mission_success_chance }

			random_list = {
				temp_var:mission_success_chance = {
					country_event = { id = FRE_Events.9 days = 1 }
				}
				temp_var:mission_failure_chance = {
					country_event = { id = FRE_Events.10 days = 1 }
				}
			}
		}

		timeout_effect = {
			country_event = { id = FRE_Events.10 days = 1 }
		}

		ai_will_do = {
			factor = 100
		}
	}

	# Mission 3: Ensure de Lattre's Successful Arrival
	FRE_Mission_De_Lattre_Arrival = {
		icon = generic_prepare_civil_war

		allowed = {
			tag = FRE
		}

		activation = {
			has_country_flag = FRE_Mission_De_Lattre_Arrival_Activated
			NOT = { has_country_flag = FRE_Mission_De_Lattre_Arrival_Completed }
		}

		available = {
			has_country_flag = FRE_De_Lattre_In_Command
			NOT = { has_country_flag = FRE_Mission_De_Lattre_Arrival_Completed }
			NOT = { has_country_flag = FRE_Mission_De_Lattre_Arrival_Failed }
			custom_trigger_tooltip = {
				tooltip = FRE_Mission_De_Lattre_Arrival_Available_tt
				always = yes
			}
		}

		selectable_mission = yes
		days_mission_timeout = 60

		cost = 100

		fire_only_once = yes

		complete_effect = {
			fre_mission_calculate_de_lattre_success = yes
			# Calculate failure chance (100 - success chance)
			set_temp_variable = { mission_failure_chance = 100 }
			subtract_from_temp_variable = { mission_failure_chance = mission_success_chance }

			random_list = {
				temp_var:mission_success_chance = {
					country_event = { id = FRE_Events.11 days = 1 }
				}
				temp_var:mission_failure_chance = {
					country_event = { id = FRE_Events.12 days = 1 }
				}
			}
		}

		timeout_effect = {
			country_event = { id = FRE_Events.12 days = 1 }
		}

		ai_will_do = {
			factor = 100
		}
	}

	# Mission 4: Prepare Na San Position
	FRE_Mission_Prepare_Na_San = {
		icon = generic_prepare_civil_war

		allowed = {
			tag = FRE
		}

		activation = {
			has_country_flag = FRE_Mission_Prepare_Na_San_Activated
			NOT = { has_country_flag = FRE_Mission_Prepare_Na_San_Completed }
		}

		available = {
			has_country_flag = FRE_Na_San_Prepared
			NOT = { has_country_flag = FRE_Mission_Prepare_Na_San_Completed }
			NOT = { has_country_flag = FRE_Mission_Prepare_Na_San_Failed }
			custom_trigger_tooltip = {
				tooltip = FRE_Mission_Prepare_Na_San_Available_tt
				always = yes
			}
		}

		selectable_mission = yes
		days_mission_timeout = 60

		cost = 100

		fire_only_once = yes

		complete_effect = {
			fre_mission_calculate_battle_prep_success = yes
			# Calculate failure chance (100 - success chance)
			set_temp_variable = { mission_failure_chance = 100 }
			subtract_from_temp_variable = { mission_failure_chance = mission_success_chance }

			random_list = {
				temp_var:mission_success_chance = {
					country_event = { id = FRE_Events.13 days = 1 }
				}
				temp_var:mission_failure_chance = {
					country_event = { id = FRE_Events.14 days = 1 }
				}
			}
		}

		timeout_effect = {
			country_event = { id = FRE_Events.14 days = 1 }
		}

		ai_will_do = {
			factor = 100
		}
	}

	# Mission 5: Prepare Vinh Yen Offensive
	FRE_Mission_Prepare_Vinh_Yen = {
		icon = generic_prepare_civil_war

		allowed = {
			tag = FRE
		}

		activation = {
			has_country_flag = FRE_Mission_Prepare_Vinh_Yen_Activated
			NOT = { has_country_flag = FRE_Mission_Prepare_Vinh_Yen_Completed }
		}

		available = {
			has_country_flag = FRE_Vinh_Yen_Prepared
			NOT = { has_country_flag = FRE_Mission_Prepare_Vinh_Yen_Completed }
			NOT = { has_country_flag = FRE_Mission_Prepare_Vinh_Yen_Failed }
			custom_trigger_tooltip = {
				tooltip = FRE_Mission_Prepare_Vinh_Yen_Available_tt
				always = yes
			}
		}

		selectable_mission = yes
		days_mission_timeout = 60

		cost = 100

		fire_only_once = yes

		complete_effect = {
			fre_mission_calculate_battle_prep_success = yes
			# Calculate failure chance (100 - success chance)
			set_temp_variable = { mission_failure_chance = 100 }
			subtract_from_temp_variable = { mission_failure_chance = mission_success_chance }

			random_list = {
				temp_var:mission_success_chance = {
					country_event = { id = FRE_Events.15 days = 1 }
				}
				temp_var:mission_failure_chance = {
					country_event = { id = FRE_Events.16 days = 1 }
				}
			}
		}

		timeout_effect = {
			country_event = { id = FRE_Events.16 days = 1 }
		}

		ai_will_do = {
			factor = 100
		}
	}

	# Mission 6: Prepare Mao Khe Position
	FRE_Mission_Prepare_Mao_Khe = {
		icon = generic_prepare_civil_war

		allowed = {
			tag = FRE
		}

		activation = {
			has_country_flag = FRE_Mission_Prepare_Mao_Khe_Activated
			NOT = { has_country_flag = FRE_Mission_Prepare_Mao_Khe_Completed }
		}

		available = {
			has_country_flag = FRE_Mao_Khe_Prepared
			NOT = { has_country_flag = FRE_Mission_Prepare_Mao_Khe_Completed }
			NOT = { has_country_flag = FRE_Mission_Prepare_Mao_Khe_Failed }
			custom_trigger_tooltip = {
				tooltip = FRE_Mission_Prepare_Mao_Khe_Available_tt
				always = yes
			}
		}

		selectable_mission = yes
		days_mission_timeout = 60

		cost = 100

		fire_only_once = yes

		complete_effect = {
			fre_mission_calculate_battle_prep_success = yes
			# Calculate failure chance (100 - success chance)
			set_temp_variable = { mission_failure_chance = 100 }
			subtract_from_temp_variable = { mission_failure_chance = mission_success_chance }

			random_list = {
				temp_var:mission_success_chance = {
					country_event = { id = FRE_Events.17 days = 1 }
				}
				temp_var:mission_failure_chance = {
					country_event = { id = FRE_Events.18 days = 1 }
				}
			}
		}

		timeout_effect = {
			country_event = { id = FRE_Events.18 days = 1 }
		}

		ai_will_do = {
			factor = 100
		}
	}

	# Mission: Operation Lorraine
	FRE_Mission_Operation_Lorraine = {
		icon = generic_offensive_operations

		allowed = {
			tag = FRE
		}

		activation = {
			has_country_flag = FRE_Mission_Operation_Lorraine_Activated
			NOT = { has_country_flag = FRE_Mission_Operation_Lorraine_Completed }
		}

		available = {
			has_country_flag = FRE_Operation_Lorraine_Launched
			country_exists = VIN
			NOT = { has_country_flag = FRE_Mission_Operation_Lorraine_Completed }
			NOT = { has_country_flag = FRE_Mission_Operation_Lorraine_Failed }
			custom_trigger_tooltip = {
				tooltip = FRE_Mission_Operation_Lorraine_Available_tt
				always = yes
			}
		}

		selectable_mission = yes
		days_mission_timeout = 90

		cost = 150

		fire_only_once = yes

		complete_effect = {
			# Start border war with VIN for Operation Lorraine
			# Find a state that FRE owns (border wars use state owner, not controller)
			# If FRE doesn't own states, fall back to random chance system
			if = {
				limit = {
					country_exists = VIN
					any_owned_state = {
						OR = {
							state = 1280
							state = 1281
							state = 1760
							state = 1761
						}
						any_neighbor_state = {
							is_owned_by = VIN
						}
					}
				}
				# Find a FRE-owned state for attacker
				random_owned_state = {
					limit = {
						OR = {
							state = 1280
							state = 1281
							state = 1760
							state = 1761
						}
						any_neighbor_state = {
							is_owned_by = VIN
						}
					}
					set_temp_variable = { attacker_state = THIS.id }
					# Find a VIN-owned neighbor state for defender
					random_neighbor_state = {
						limit = {
							is_owned_by = VIN
						}
						set_temp_variable = { defender_state = THIS.id }
					}
				}
				start_border_war = {
					change_state_after_war = no
					combat_width = 80
					minimum_duration_in_days = 21
					attacker = {
						state = var:attacker_state
						num_provinces = 6
						on_win = FRE_Events.24
						on_lose = FRE_Events.25
						on_cancel = FRE_Events.25
						modifier = 0.3
					}
					defender = {
						state = var:defender_state
						num_provinces = 6
						on_win = FRE_Events.25
						on_lose = FRE_Events.24
						on_cancel = FRE_Events.25
					}
				}
			}
			else = {
				# If border war can't start, use random chance based on metrics
				fre_mission_calculate_battle_prep_success = yes
				set_temp_variable = { mission_failure_chance = 100 }
				subtract_from_temp_variable = { mission_failure_chance = mission_success_chance }
				random_list = {
					temp_var:mission_success_chance = {
						country_event = { id = FRE_Events.24 days = 1 }
					}
					temp_var:mission_failure_chance = {
						country_event = { id = FRE_Events.25 days = 1 }
					}
				}
			}
		}

		timeout_effect = {
			country_event = { id = FRE_Events.25 days = 1 }
		}

		ai_will_do = {
			factor = 100
		}
	}

	# Mission: Operation Hirondelle
	FRE_Mission_Operation_Hirondelle = {
		icon = generic_air_operations

		allowed = {
			tag = FRE
		}

		activation = {
			has_country_flag = FRE_Mission_Operation_Hirondelle_Activated
			NOT = { has_country_flag = FRE_Mission_Operation_Hirondelle_Completed }
		}

		available = {
			has_country_flag = FRE_Operation_Hirondelle_Launched
			NOT = { has_country_flag = FRE_Mission_Operation_Hirondelle_Completed }
			NOT = { has_country_flag = FRE_Mission_Operation_Hirondelle_Failed }
			OR = {
				has_equipment = { transport_plane_equipment > 10 }
				has_country_flag = FRE_Air_Assets_Delivered
			}
			custom_trigger_tooltip = {
				tooltip = FRE_Mission_Operation_Hirondelle_Available_tt
				always = yes
			}
		}

		selectable_mission = yes
		days_mission_timeout = 60

		cost = 100

		fire_only_once = yes

		complete_effect = {
			# Success based on air superiority, transport planes, and paratrooper units
			set_temp_variable = { mission_success_chance = 50 }
			# Air experience helps
			if = {
				limit = { check_variable = { air_experience > 100 } }
				add_to_temp_variable = { mission_success_chance = 15 }
			}
			# Transport planes help
			if = {
				limit = { has_equipment = { transport_plane_equipment > 20 } }
				add_to_temp_variable = { mission_success_chance = 20 }
			}
			# Political power helps
			if = {
				limit = { check_variable = { political_power > 100 } }
				add_to_temp_variable = { mission_success_chance = 15 }
			}
			# Clamp
			if = {
				limit = { check_variable = { temp.mission_success_chance > 100 } }
				set_temp_variable = { mission_success_chance = 100 }
			}
			set_temp_variable = { mission_failure_chance = 100 }
			subtract_from_temp_variable = { mission_failure_chance = mission_success_chance }
			random_list = {
				temp_var:mission_success_chance = {
					country_event = { id = FRE_Events.26 days = 1 }
				}
				temp_var:mission_failure_chance = {
					country_event = { id = FRE_Events.27 days = 1 }
				}
			}
		}

		timeout_effect = {
			country_event = { id = FRE_Events.27 days = 1 }
		}

		ai_will_do = {
			factor = 100
		}
	}

	# Mission: Operation Mouette
	FRE_Mission_Operation_Mouette = {
		icon = generic_naval_operations

		allowed = {
			tag = FRE
		}

		activation = {
			has_country_flag = FRE_Mission_Operation_Mouette_Activated
			NOT = { has_country_flag = FRE_Mission_Operation_Mouette_Completed }
		}

		available = {
			has_country_flag = FRE_Operation_Mouette_Launched
			NOT = { has_country_flag = FRE_Mission_Operation_Mouette_Completed }
			NOT = { has_country_flag = FRE_Mission_Operation_Mouette_Failed }
			OR = {
				has_navy_size = { size > 10 }
				has_country_flag = FRE_Dinassaut_Flotilla
			}
			custom_trigger_tooltip = {
				tooltip = FRE_Mission_Operation_Mouette_Available_tt
				always = yes
			}
		}

		selectable_mission = yes
		days_mission_timeout = 75

		cost = 125

		fire_only_once = yes

		complete_effect = {
			# Naval-based mission success
			set_temp_variable = { mission_success_chance = 45 }
			# Naval experience helps
			if = {
				limit = { check_variable = { naval_experience > 50 } }
				add_to_temp_variable = { mission_success_chance = 20 }
			}
			# Navy size helps
			if = {
				limit = { has_navy_size = { size > 20 } }
				add_to_temp_variable = { mission_success_chance = 15 }
			}
			# Political power helps
			if = {
				limit = { check_variable = { political_power > 100 } }
				add_to_temp_variable = { mission_success_chance = 20 }
			}
			# Clamp
			if = {
				limit = { check_variable = { temp.mission_success_chance > 100 } }
				set_temp_variable = { mission_success_chance = 100 }
			}
			set_temp_variable = { mission_failure_chance = 100 }
			subtract_from_temp_variable = { mission_failure_chance = mission_success_chance }
			random_list = {
				temp_var:mission_success_chance = {
					country_event = { id = FRE_Events.28 days = 1 }
				}
				temp_var:mission_failure_chance = {
					country_event = { id = FRE_Events.29 days = 1 }
				}
			}
		}

		timeout_effect = {
			country_event = { id = FRE_Events.29 days = 1 }
		}

		ai_will_do = {
			factor = 100
		}
	}

	# Mission: Operation Brochet
	FRE_Mission_Operation_Brochet = {
		icon = generic_offensive_operations

		allowed = {
			tag = FRE
		}

		activation = {
			has_country_flag = FRE_Mission_Operation_Brochet_Activated
			NOT = { has_country_flag = FRE_Mission_Operation_Brochet_Completed }
		}

		available = {
			has_country_flag = FRE_Operation_Brochet_Launched
			country_exists = VIN
			NOT = { has_country_flag = FRE_Mission_Operation_Brochet_Completed }
			NOT = { has_country_flag = FRE_Mission_Operation_Brochet_Failed }
			custom_trigger_tooltip = {
				tooltip = FRE_Mission_Operation_Brochet_Available_tt
				always = yes
			}
		}

		selectable_mission = yes
		days_mission_timeout = 90

		cost = 125

		fire_only_once = yes

		complete_effect = {
			# Start border war with VIN for Operation Brochet
			# Find a state that FRE owns for the battle
			if = {
				limit = {
					country_exists = VIN
					any_owned_state = {
						OR = {
							state = 1280
							state = 1281
							state = 1760
							state = 1761
						}
						any_neighbor_state = {
							is_owned_by = VIN
						}
					}
				}
				# Find a FRE-owned state for attacker
				random_owned_state = {
					limit = {
						OR = {
							state = 1280
							state = 1281
							state = 1760
							state = 1761
						}
						any_neighbor_state = {
							is_owned_by = VIN
						}
					}
					set_temp_variable = { attacker_state = THIS.id }
					# Find a VIN-owned neighbor state for defender
					random_neighbor_state = {
						limit = {
							is_owned_by = VIN
						}
						set_temp_variable = { defender_state = THIS.id }
					}
				}
				start_border_war = {
					change_state_after_war = no
					combat_width = 80
					minimum_duration_in_days = 21
					attacker = {
						state = var:attacker_state
						num_provinces = 5
						on_win = FRE_Events.30
						on_lose = FRE_Events.31
						on_cancel = FRE_Events.31
						modifier = 0.25
					}
					defender = {
						state = var:defender_state
						num_provinces = 5
						on_win = FRE_Events.31
						on_lose = FRE_Events.30
						on_cancel = FRE_Events.31
					}
				}
			}
			else = {
				# Fallback to random chance
				fre_mission_calculate_battle_prep_success = yes
				set_temp_variable = { mission_failure_chance = 100 }
				subtract_from_temp_variable = { mission_failure_chance = mission_success_chance }
				random_list = {
					temp_var:mission_success_chance = {
						country_event = { id = FRE_Events.30 days = 1 }
					}
					temp_var:mission_failure_chance = {
						country_event = { id = FRE_Events.31 days = 1 }
					}
				}
			}
		}

		timeout_effect = {
			country_event = { id = FRE_Events.31 days = 1 }
		}

		ai_will_do = {
			factor = 100
		}
	}

	# Mission: Operation Castor
	FRE_Mission_Operation_Castor = {
		icon = generic_air_operations

		allowed = {
			tag = FRE
		}

		activation = {
			has_country_flag = FRE_Mission_Operation_Castor_Activated
			NOT = { has_country_flag = FRE_Mission_Operation_Castor_Completed }
		}

		available = {
			has_country_flag = FRE_Operation_Castor_Launched
			country_exists = VIN
			NOT = { has_country_flag = FRE_Mission_Operation_Castor_Completed }
			NOT = { has_country_flag = FRE_Mission_Operation_Castor_Failed }
			OR = {
				has_equipment = { transport_plane_equipment > 15 }
				has_country_flag = FRE_Air_Assets_Delivered
			}
			custom_trigger_tooltip = {
				tooltip = FRE_Mission_Operation_Castor_Available_tt
				always = yes
			}
		}

		selectable_mission = yes
		days_mission_timeout = 90

		cost = 200

		fire_only_once = yes

		complete_effect = {
			# Start border war for Dien Bien Phu (Operation Castor)
			# Find a state that FRE owns for the critical battle
			if = {
				limit = {
					country_exists = VIN
					any_owned_state = {
						OR = {
							state = 1280
							state = 1281
							state = 1760
							state = 1761
						}
						any_neighbor_state = {
							is_owned_by = VIN
						}
					}
				}
				# Find a FRE-owned state for attacker (prefer 1760 for Dien Bien Phu)
				random_owned_state = {
					prioritize = { 1760 }
					limit = {
						OR = {
							state = 1280
							state = 1281
							state = 1760
							state = 1761
						}
						any_neighbor_state = {
							is_owned_by = VIN
						}
					}
					set_temp_variable = { attacker_state = THIS.id }
					# Find a VIN-owned neighbor state for defender
					random_neighbor_state = {
						limit = {
							is_owned_by = VIN
						}
						set_temp_variable = { defender_state = THIS.id }
					}
				}
				start_border_war = {
					change_state_after_war = no
					combat_width = 80
					minimum_duration_in_days = 28
					attacker = {
						state = var:attacker_state
						num_provinces = 4
						on_win = FRE_Events.32
						on_lose = FRE_Events.33
						on_cancel = FRE_Events.33
						modifier = 0.2
					}
					defender = {
						state = var:defender_state
						num_provinces = 4
						on_win = FRE_Events.33
						on_lose = FRE_Events.32
						on_cancel = FRE_Events.33
					}
				}
			}
			else = {
				# Fallback - Castor is critical, so lower base chance
				set_temp_variable = { mission_success_chance = 35 }
				if = {
					limit = { check_variable = { air_experience > 100 } }
					add_to_temp_variable = { mission_success_chance = 20 }
				}
				if = {
					limit = { has_equipment = { transport_plane_equipment > 25 } }
					add_to_temp_variable = { mission_success_chance = 25 }
				}
				if = {
					limit = { check_variable = { political_power > 150 } }
					add_to_temp_variable = { mission_success_chance = 20 }
				}
				if = {
					limit = { check_variable = { temp.mission_success_chance > 100 } }
					set_temp_variable = { mission_success_chance = 100 }
				}
				set_temp_variable = { mission_failure_chance = 100 }
				subtract_from_temp_variable = { mission_failure_chance = mission_success_chance }
				random_list = {
					temp_var:mission_success_chance = {
						country_event = { id = FRE_Events.32 days = 1 }
					}
					temp_var:mission_failure_chance = {
						country_event = { id = FRE_Events.33 days = 1 }
					}
				}
			}
		}

		timeout_effect = {
			country_event = { id = FRE_Events.33 days = 1 }
		}

		ai_will_do = {
			factor = 100
		}
	}

	# ------------------------------------------------------------------
	# The de Lattre Line: fortifying the Tonkin delta (states 1760, 786).
	# Only available once de Lattre (or a successor) commands the corps.
	# ------------------------------------------------------------------
	FRE_Fortify_Delta_1 = {
		icon = generic_construction

		allowed = {
			tag = FRE
		}

		visible = {
			NOT = { has_idea = FRE_Command_Carpentier }
			NOT = { has_country_flag = FRE_CEFEO_Dissolved }
		}

		available = {
			has_equipment = { support_equipment > 500 }
			controls_state = 1760
		}

		cost = 25

		fire_only_once = yes

		complete_effect = {
			add_equipment_to_stockpile = {
				type = support_equipment
				amount = -500
			}
			1760 = {
				add_building_construction = {
					type = bunker
					level = 2
					instant_build = yes
					province = {
						all_provinces = yes
						limit_to_border = yes
					}
				}
			}
			set_country_flag = FRE_Fortified_Delta_1
		}

		ai_will_do = {
			factor = 20
		}
	}

	FRE_Fortify_Delta_2 = {
		icon = generic_construction

		allowed = {
			tag = FRE
		}

		visible = {
			has_completed_focus = FRE_Send_De_lattre_de_Tassigny
			NOT = { has_idea = FRE_Command_Carpentier }
			NOT = { has_country_flag = FRE_CEFEO_Dissolved }
		}

		available = {
			has_country_flag = FRE_Fortified_Delta_1
			has_equipment = { support_equipment > 500 }
			controls_state = 786
		}

		cost = 25

		fire_only_once = yes

		complete_effect = {
			add_equipment_to_stockpile = {
				type = support_equipment
				amount = -500
			}
			786 = {
				add_building_construction = {
					type = bunker
					level = 2
					instant_build = yes
					province = {
						all_provinces = yes
						limit_to_border = yes
					}
				}
			}
			set_country_flag = FRE_Fortified_Delta_2
		}

		ai_will_do = {
			factor = 20
		}
	}

	FRE_Fortify_Delta_3 = {
		icon = generic_construction

		allowed = {
			tag = FRE
		}

		visible = {
			has_completed_focus = FRE_Send_De_lattre_de_Tassigny
			NOT = { has_idea = FRE_Command_Carpentier }
			NOT = { has_country_flag = FRE_CEFEO_Dissolved }
		}

		available = {
			has_country_flag = FRE_Fortified_Delta_2
			has_equipment = { support_equipment > 750 }
			controls_state = 1760
		}

		cost = 50

		fire_only_once = yes

		complete_effect = {
			add_equipment_to_stockpile = {
				type = support_equipment
				amount = -750
			}
			1760 = {
				add_building_construction = {
					type = bunker
					level = 3
					instant_build = yes
					province = {
						all_provinces = yes
						limit_to_border = yes
					}
				}
			}
			786 = {
				add_building_construction = {
					type = bunker
					level = 1
					instant_build = yes
					province = {
						all_provinces = yes
						limit_to_border = yes
					}
				}
			}
			# The finished Line steadies the whole delta garrison
			add_war_support = 0.05
		}

		ai_will_do = {
			factor = 20
		}
	}
}

# =============================================================================
# CEFEO lifeline: requests to the Metropole
# =============================================================================
# Each request costs PP and a little Metropole Patience, then fires an
# FRA-side event (FRE_Metropole namespace) where Paris grants, half-grants or
# refuses. If FRA is gone, a reduced automatic grant is applied instead so the
# corps is never soft-locked.
# =============================================================================

FRE_Metropole_Support_category = {

	FRE_Request_Equipment_Convoy = {
		icon = generic_industry

		allowed = {
			tag = FRE
		}

		visible = {
			NOT = { has_country_flag = FRE_CEFEO_Dissolved }
		}

		available = {
			NOT = { has_country_flag = FRE_Metropole_Request_Pending }
		}

		cost = 25

		fire_only_once = no
		days_re_enable = 60

		complete_effect = {
			set_temp_variable = { fre_patience_delta = -3 }
			fre_metropole_patience_change = yes
			if = {
				limit = { country_exists = FRA }
				# Timed so a vanished FRA can never wedge the request system
			set_country_flag = { flag = FRE_Metropole_Request_Pending days = 30 value = 1 }
				FRA = {
					country_event = { id = FRE_Metropole.1 days = 3 }
				}
			}
			else = {
				# No Metropole left to ask: scrape together theatre reserves
				add_equipment_to_stockpile = {
					type = infantry_equipment_1
					amount = 6000
					producer = FRA
				}
				add_equipment_to_stockpile = {
					type = support_equipment
					amount = 750
					producer = FRA
				}
			}
		}

		ai_will_do = {
			factor = 10
			modifier = {
				factor = 0
				has_equipment = { infantry_equipment_1 > 15000 }
			}
		}
	}

	FRE_Request_Reinforcements = {
		icon = generic_manpower

		allowed = {
			tag = FRE
		}

		visible = {
			NOT = { has_country_flag = FRE_CEFEO_Dissolved }
		}

		available = {
			NOT = { has_country_flag = FRE_Metropole_Request_Pending }
		}

		cost = 40

		fire_only_once = no
		days_re_enable = 90

		complete_effect = {
			set_temp_variable = { fre_patience_delta = -4 }
			fre_metropole_patience_change = yes
			if = {
				limit = { country_exists = FRA }
				# Timed so a vanished FRA can never wedge the request system
			set_country_flag = { flag = FRE_Metropole_Request_Pending days = 30 value = 1 }
				FRA = {
					country_event = { id = FRE_Metropole.2 days = 3 }
				}
			}
			else = {
				add_manpower = 8000
			}
		}

		ai_will_do = {
			factor = 8
			modifier = {
				factor = 0
				has_manpower > 30000
			}
		}
	}

	FRE_Request_Air_Assets = {
		icon = generic_air

		allowed = {
			tag = FRE
		}

		visible = {
			NOT = { has_country_flag = FRE_CEFEO_Dissolved }
		}

		available = {
			NOT = { has_country_flag = FRE_Metropole_Request_Pending }
		}

		cost = 35

		fire_only_once = no
		days_re_enable = 120

		complete_effect = {
			set_temp_variable = { fre_patience_delta = -4 }
			fre_metropole_patience_change = yes
			if = {
				limit = { country_exists = FRA }
				# Timed so a vanished FRA can never wedge the request system
			set_country_flag = { flag = FRE_Metropole_Request_Pending days = 30 value = 1 }
				FRA = {
					country_event = { id = FRE_Metropole.3 days = 3 }
				}
			}
			else = {
				add_equipment_to_stockpile = {
					type = transport_plane_equipment
					amount = 12
					producer = FRA
				}
				set_country_flag = FRE_Air_Assets_Delivered
			}
		}

		ai_will_do = {
			factor = 6
			modifier = {
				factor = 0
				has_country_flag = FRE_Air_Assets_Delivered
			}
		}
	}

	FRE_Request_Naval_Assets = {
		icon = generic_naval

		allowed = {
			tag = FRE
		}

		visible = {
			NOT = { has_country_flag = FRE_CEFEO_Dissolved }
		}

		available = {
			NOT = { has_country_flag = FRE_Metropole_Request_Pending }
			NOT = { has_country_flag = FRE_Dinassaut_Flotilla }
		}

		cost = 35

		fire_only_once = no
		days_re_enable = 120

		complete_effect = {
			set_temp_variable = { fre_patience_delta = -4 }
			fre_metropole_patience_change = yes
			if = {
				limit = { country_exists = FRA }
				# Timed so a vanished FRA can never wedge the request system
			set_country_flag = { flag = FRE_Metropole_Request_Pending days = 30 value = 1 }
				FRA = {
					country_event = { id = FRE_Metropole.4 days = 3 }
				}
			}
			else = {
				set_country_flag = FRE_Dinassaut_Flotilla
				navy_experience = 25
			}
		}

		ai_will_do = {
			factor = 6
			modifier = {
				factor = 0
				has_country_flag = FRE_Dinassaut_Flotilla
			}
		}
	}

	FRE_Request_Administrative_Support = {
		icon = generic_political_discourse

		allowed = {
			tag = FRE
		}

		visible = {
			NOT = { has_country_flag = FRE_CEFEO_Dissolved }
		}

		available = {
			NOT = { has_country_flag = FRE_Metropole_Request_Pending }
		}

		cost = 0

		fire_only_once = no
		days_re_enable = 90

		complete_effect = {
			set_temp_variable = { fre_patience_delta = -3 }
			fre_metropole_patience_change = yes
			if = {
				limit = { country_exists = FRA }
				# Timed so a vanished FRA can never wedge the request system
			set_country_flag = { flag = FRE_Metropole_Request_Pending days = 30 value = 1 }
				FRA = {
					country_event = { id = FRE_Metropole.5 days = 3 }
				}
			}
			else = {
				add_political_power = 40
			}
		}

		ai_will_do = {
			factor = 5
			modifier = {
				factor = 0
				check_variable = { political_power > 150 }
			}
		}
	}

	FRE_Appeal_To_Paris_Press = {
		icon = generic_propaganda

		allowed = {
			tag = FRE
		}

		visible = {
			NOT = { has_country_flag = FRE_CEFEO_Dissolved }
		}

		available = {
			country_exists = FRA
		}

		cost = 15

		fire_only_once = no
		days_re_enable = 180

		complete_effect = {
			custom_effect_tooltip = FRE_Appeal_To_Paris_Press_tt
			hidden_effect = {
				random_list = {
					60 = {
						set_temp_variable = { fre_patience_delta = 10 }
						fre_metropole_patience_change = yes
						add_war_support = 0.03
					}
					40 = {
						set_temp_variable = { fre_patience_delta = -8 }
						fre_metropole_patience_change = yes
						if = {
							limit = { country_exists = FRA }
							FRA = {
								add_stability = -0.02
							}
						}
					}
				}
			}
		}

		ai_will_do = {
			factor = 2
			modifier = {
				factor = 0
				check_variable = { FRE_Metropole_Patience > 40 }
			}
		}
	}

	# Manual wind-down: hand the theatre over and bring the corps home.
	FRE_Dissolve_The_CEFEO = {
		icon = generic_civil_support

		allowed = {
			tag = FRE
		}

		visible = {
			NOT = { has_country_flag = FRE_CEFEO_Dissolved }
			NOT = { has_country_flag = FRE_Winddown_Started }
			OR = {
				has_global_flag = Indochina_War_Over
				check_variable = { FRE_Metropole_Patience < 0.01 }
			}
		}

		cost = 0

		fire_only_once = yes

		complete_effect = {
			set_country_flag = FRE_Winddown_Started
			country_event = { id = FRE_Winddown.2 days = 2 }
		}

		ai_will_do = {
			# AI relies on the automatic wind-down in fre_weekly_pulse instead
			factor = 0
		}
	}
}

# =============================================================================
# CEFEO lifeline: requests to the Metropole
# =============================================================================
# Each request costs PP and a little Metropole Patience, then fires an
# FRA-side event (FRE_Metropole namespace) where Paris grants, half-grants or
# refuses. If FRA is gone, a reduced automatic grant is applied instead so the
# corps is never soft-locked.
# =============================================================================

FRE_Metropole_Support_category = {

	FRE_Request_Equipment_Convoy = {
		icon = generic_industry

		allowed = {
			tag = FRE
		}

		visible = {
			NOT = { has_country_flag = FRE_CEFEO_Dissolved }
		}

		available = {
			NOT = { has_country_flag = FRE_Metropole_Request_Pending }
		}

		cost = 25

		fire_only_once = no
		days_re_enable = 60

		complete_effect = {
			set_temp_variable = { fre_patience_delta = -3 }
			fre_metropole_patience_change = yes
			if = {
				limit = { country_exists = FRA }
				# Timed so a vanished FRA can never wedge the request system
			set_country_flag = { flag = FRE_Metropole_Request_Pending days = 30 value = 1 }
				FRA = {
					country_event = { id = FRE_Metropole.1 days = 3 }
				}
			}
			else = {
				# No Metropole left to ask: scrape together theatre reserves
				add_equipment_to_stockpile = {
					type = infantry_equipment_1
					amount = 6000
					producer = FRA
				}
				add_equipment_to_stockpile = {
					type = support_equipment
					amount = 750
					producer = FRA
				}
			}
		}

		ai_will_do = {
			factor = 10
			modifier = {
				factor = 0
				has_equipment = { infantry_equipment_1 > 15000 }
			}
		}
	}

	FRE_Request_Reinforcements = {
		icon = generic_manpower

		allowed = {
			tag = FRE
		}

		visible = {
			NOT = { has_country_flag = FRE_CEFEO_Dissolved }
		}

		available = {
			NOT = { has_country_flag = FRE_Metropole_Request_Pending }
		}

		cost = 40

		fire_only_once = no
		days_re_enable = 90

		complete_effect = {
			set_temp_variable = { fre_patience_delta = -4 }
			fre_metropole_patience_change = yes
			if = {
				limit = { country_exists = FRA }
				# Timed so a vanished FRA can never wedge the request system
			set_country_flag = { flag = FRE_Metropole_Request_Pending days = 30 value = 1 }
				FRA = {
					country_event = { id = FRE_Metropole.2 days = 3 }
				}
			}
			else = {
				add_manpower = 8000
			}
		}

		ai_will_do = {
			factor = 8
			modifier = {
				factor = 0
				has_manpower > 30000
			}
		}
	}

	FRE_Request_Air_Assets = {
		icon = generic_air

		allowed = {
			tag = FRE
		}

		visible = {
			NOT = { has_country_flag = FRE_CEFEO_Dissolved }
		}

		available = {
			NOT = { has_country_flag = FRE_Metropole_Request_Pending }
		}

		cost = 35

		fire_only_once = no
		days_re_enable = 120

		complete_effect = {
			set_temp_variable = { fre_patience_delta = -4 }
			fre_metropole_patience_change = yes
			if = {
				limit = { country_exists = FRA }
				# Timed so a vanished FRA can never wedge the request system
			set_country_flag = { flag = FRE_Metropole_Request_Pending days = 30 value = 1 }
				FRA = {
					country_event = { id = FRE_Metropole.3 days = 3 }
				}
			}
			else = {
				add_equipment_to_stockpile = {
					type = transport_plane_equipment
					amount = 12
					producer = FRA
				}
				set_country_flag = FRE_Air_Assets_Delivered
			}
		}

		ai_will_do = {
			factor = 6
			modifier = {
				factor = 0
				has_country_flag = FRE_Air_Assets_Delivered
			}
		}
	}

	FRE_Request_Naval_Assets = {
		icon = generic_naval

		allowed = {
			tag = FRE
		}

		visible = {
			NOT = { has_country_flag = FRE_CEFEO_Dissolved }
		}

		available = {
			NOT = { has_country_flag = FRE_Metropole_Request_Pending }
			NOT = { has_country_flag = FRE_Dinassaut_Flotilla }
		}

		cost = 35

		fire_only_once = no
		days_re_enable = 120

		complete_effect = {
			set_temp_variable = { fre_patience_delta = -4 }
			fre_metropole_patience_change = yes
			if = {
				limit = { country_exists = FRA }
				# Timed so a vanished FRA can never wedge the request system
			set_country_flag = { flag = FRE_Metropole_Request_Pending days = 30 value = 1 }
				FRA = {
					country_event = { id = FRE_Metropole.4 days = 3 }
				}
			}
			else = {
				set_country_flag = FRE_Dinassaut_Flotilla
				navy_experience = 25
			}
		}

		ai_will_do = {
			factor = 6
			modifier = {
				factor = 0
				has_country_flag = FRE_Dinassaut_Flotilla
			}
		}
	}

	FRE_Request_Administrative_Support = {
		icon = generic_political_discourse

		allowed = {
			tag = FRE
		}

		visible = {
			NOT = { has_country_flag = FRE_CEFEO_Dissolved }
		}

		available = {
			NOT = { has_country_flag = FRE_Metropole_Request_Pending }
		}

		cost = 0

		fire_only_once = no
		days_re_enable = 90

		complete_effect = {
			set_temp_variable = { fre_patience_delta = -3 }
			fre_metropole_patience_change = yes
			if = {
				limit = { country_exists = FRA }
				# Timed so a vanished FRA can never wedge the request system
			set_country_flag = { flag = FRE_Metropole_Request_Pending days = 30 value = 1 }
				FRA = {
					country_event = { id = FRE_Metropole.5 days = 3 }
				}
			}
			else = {
				add_political_power = 40
			}
		}

		ai_will_do = {
			factor = 5
			modifier = {
				factor = 0
				check_variable = { political_power > 150 }
			}
		}
	}

	FRE_Appeal_To_Paris_Press = {
		icon = generic_propaganda

		allowed = {
			tag = FRE
		}

		visible = {
			NOT = { has_country_flag = FRE_CEFEO_Dissolved }
		}

		available = {
			country_exists = FRA
		}

		cost = 15

		fire_only_once = no
		days_re_enable = 180

		complete_effect = {
			custom_effect_tooltip = FRE_Appeal_To_Paris_Press_tt
			hidden_effect = {
				random_list = {
					60 = {
						set_temp_variable = { fre_patience_delta = 10 }
						fre_metropole_patience_change = yes
						add_war_support = 0.03
					}
					40 = {
						set_temp_variable = { fre_patience_delta = -8 }
						fre_metropole_patience_change = yes
						if = {
							limit = { country_exists = FRA }
							FRA = {
								add_stability = -0.02
							}
						}
					}
				}
			}
		}

		ai_will_do = {
			factor = 2
			modifier = {
				factor = 0
				check_variable = { FRE_Metropole_Patience > 40 }
			}
		}
	}

	# Manual wind-down: hand the theatre over and bring the corps home.
	FRE_Dissolve_The_CEFEO = {
		icon = generic_civil_support

		allowed = {
			tag = FRE
		}

		visible = {
			NOT = { has_country_flag = FRE_CEFEO_Dissolved }
			NOT = { has_country_flag = FRE_Winddown_Started }
			OR = {
				has_global_flag = Indochina_War_Over
				check_variable = { FRE_Metropole_Patience < 0.01 }
			}
		}

		cost = 0

		fire_only_once = yes

		complete_effect = {
			set_country_flag = FRE_Winddown_Started
			country_event = { id = FRE_Winddown.2 days = 2 }
		}

		ai_will_do = {
			# AI relies on the automatic wind-down in fre_weekly_pulse instead
			factor = 0
		}
	}
}

# =============================================================================
# CEFEO in-theatre coordination: the State of Vietnam, the montagnard
# auxiliaries (GCMA) and Saigon. Unlocked by the Brothers in Arms focus branch.
# =============================================================================

FRE_Indochina_Allies_category = {

	# Jaunissement: train and expand the Vietnamese National Army.
	FRE_Train_Vietnamese_National_Army = {
		icon = generic_manpower

		allowed = {
			tag = FRE
		}

		visible = {
			has_country_flag = FRE_Jaunissement_Started
			NOT = { has_country_flag = FRE_CEFEO_Dissolved }
		}

		available = {
			country_exists = VIE
			has_equipment = { infantry_equipment_1 > 5000 }
			NOT = { has_country_flag = FRE_VNA_Training_Pending }
		}

		cost = 25

		fire_only_once = no
		days_re_enable = 90

		complete_effect = {
			set_country_flag = FRE_VNA_Training_Pending
			VIE = {
				country_event = { id = FRE_Allies.1 days = 3 }
			}
		}

		ai_will_do = {
			factor = 5
			modifier = {
				factor = 0
				has_equipment = { infantry_equipment_1 < 12000 }
			}
		}
	}

	# GCMA auxiliary networks among the highland peoples.
	FRE_GCMA_Recruit_TAM = {
		icon = generic_prepare_civil_war

		allowed = {
			tag = FRE
		}

		visible = {
			has_country_flag = FRE_GCMA_Unlocked
			NOT = { has_country_flag = FRE_CEFEO_Dissolved }
		}

		available = {
			country_exists = TAM
			has_equipment = { support_equipment > 250 }
		}

		cost = 25

		fire_only_once = yes

		complete_effect = {
			add_equipment_to_stockpile = {
				type = support_equipment
				amount = -250
			}
			fre_gcma_expand_network = yes
			if = {
				limit = { country_exists = TAM }
				TAM = {
					add_opinion_modifier = {
						target = FRE
						modifier = FRE_Military_Coordination
					}
				}
			}
		}

		ai_will_do = {
			factor = 5
		}
	}

	FRE_GCMA_Recruit_TAI = {
		icon = generic_prepare_civil_war

		allowed = {
			tag = FRE
		}

		visible = {
			has_country_flag = FRE_GCMA_Unlocked
			NOT = { has_country_flag = FRE_CEFEO_Dissolved }
		}

		available = {
			country_exists = TAI
			has_equipment = { support_equipment > 250 }
		}

		cost = 25

		fire_only_once = yes

		complete_effect = {
			add_equipment_to_stockpile = {
				type = support_equipment
				amount = -250
			}
			fre_gcma_expand_network = yes
			if = {
				limit = { country_exists = TAI }
				TAI = {
					add_opinion_modifier = {
						target = FRE
						modifier = FRE_Military_Coordination
					}
				}
			}
		}

		ai_will_do = {
			factor = 5
		}
	}

	FRE_GCMA_Recruit_NUN = {
		icon = generic_prepare_civil_war

		allowed = {
			tag = FRE
		}

		visible = {
			has_country_flag = FRE_GCMA_Unlocked
			NOT = { has_country_flag = FRE_CEFEO_Dissolved }
		}

		available = {
			country_exists = NUN
			has_equipment = { support_equipment > 250 }
		}

		cost = 25

		fire_only_once = yes

		complete_effect = {
			add_equipment_to_stockpile = {
				type = support_equipment
				amount = -250
			}
			fre_gcma_expand_network = yes
			if = {
				limit = { country_exists = NUN }
				NUN = {
					add_opinion_modifier = {
						target = FRE
						modifier = FRE_Military_Coordination
					}
				}
			}
		}

		ai_will_do = {
			factor = 5
		}
	}

	FRE_GCMA_Recruit_THO = {
		icon = generic_prepare_civil_war

		allowed = {
			tag = FRE
		}

		visible = {
			has_country_flag = FRE_GCMA_Unlocked
			NOT = { has_country_flag = FRE_CEFEO_Dissolved }
		}

		available = {
			country_exists = THO
			has_equipment = { support_equipment > 250 }
		}

		cost = 25

		fire_only_once = yes

		complete_effect = {
			add_equipment_to_stockpile = {
				type = support_equipment
				amount = -250
			}
			fre_gcma_expand_network = yes
			if = {
				limit = { country_exists = THO }
				THO = {
					add_opinion_modifier = {
						target = FRE
						modifier = FRE_Military_Coordination
					}
				}
			}
		}

		ai_will_do = {
			factor = 5
		}
	}
}
