NEP_Pajani_System = {
    enable = { }
    icon = GFX_idea_pajani

    # Core “rent extraction / gatekept finance & admin”
    consumer_goods_factor = NEP_PAJANI_consumer_goods_var        # + raises CG burden
    education_spending_modifier = NEP_PAJANI_education_spend_var       # + raises state education outlays
    public_services_spending_modifier = NEP_PAJANI_public_services_spend_var # + raises bureaucracy overhead
    welfare_spending_modifier = NEP_PAJANI_welfare_spend_var         # + raises patronage-style transfers

    # Finance & business capture
    office_park_income_bonus = NEP_PAJANI_finance_income_var        # + boosts finance/commercial rents
    industrial_park_income_bonus = NEP_PAJANI_industry_patronage_var    # +/- nobles capture industry rents
    tax_rate_corporate = NEP_PAJANI_corporate_tax_var         # +/- preferential rates & exemptions
    tax_rate_upper_class = NEP_PAJANI_upper_tax_var             # +/- negotiated elite taxation

    # Labor market drag
    wage_factor = NEP_PAJANI_wage_factor_var           # - suppresses wages via patronage
    workforce_ratio = NEP_PAJANI_workforce_ratio_var       # - allocative inefficiency
}