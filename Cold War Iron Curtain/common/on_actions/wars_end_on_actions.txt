on_actions = {
	on_state_control_changed = {
		#ROOT = new controller country
		#FROM = old controller country
		#FROM.FROM is state ID
		effect = {
			if = {
				limit = {
					ROOT = {
						OR = {
							original_tag = SOV
							is_ally_with = SOV
						}
					}
					FROM = {
						original_tag = FRA
					}
					FROM.FROM = {
						state = 16
					}
				}
				news_event = soviets_ww3_victory.2
			}
			if = {
				limit = {
					has_global_flag = MAL_EMERGENCY_MLA_Victory
					FROM = {					
						original_tag = SGP
					}
					ROOT = {
						original_tag = MLA
					}
					FROM.FROM = {
						state = 336
					}
				}
				ROOT = {
					country_event = MLA.0
					white_peace = SGP
				}
				336 = {
					transfer_state_to = MLA
				}
			}
			if = {
				limit = {
					has_global_flag = Suez_Crisis
					ROOT = {
						OR = {
							original_tag = ISR
							original_tag = ENG
							original_tag = FRA
						}
					}
					FROM = {
						original_tag = EGY
					}
					FROM.FROM = {
						state = 1279
					}
					805 = {
						OR = {
							is_fully_controlled_by = ISR
							is_fully_controlled_by = ENG
							is_fully_controlled_by = FRA
						}
					}
					453 = {
						OR = {
							is_fully_controlled_by = ISR
							is_fully_controlled_by = ENG
							is_fully_controlled_by = FRA
						}
					}
				}
				ISR = { country_event = operationkadesh.5 }
			}
			if = {
				limit = {
					has_global_flag = Suez_Crisis
					ROOT = {
						original_tag = EGY
					}
					FROM = {
						original_tag = ISR
					}
					FROM.FROM = {
						state = 1296
					}
					755 = {
						is_fully_controlled_by = EGY
					}
				}
				ISR = { country_event = suez.30 }
			}
		}
	}
}
#Add Missions for Turkey/Iraq for that War