on_actions = {
	on_startup = {
		effect = {
            every_country = {
				if = {
					limit = { tag = COL }
					# PLACEHOLDER: Event col.1 does not exist - needs to be created
					# country_event = { id = col.1 days = 52 }
                }
        }
        }
    }
}