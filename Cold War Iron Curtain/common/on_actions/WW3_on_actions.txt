on_actions = {
	#ROOT is attacker, FROM is defender
	on_declare_war = {
		effect = {
			if = {
				limit = {
					OR = {
						AND = {
							ROOT = { is_in_faction_with = SOV }
							FROM = { is_in_faction_with = NAT }
						}
						AND = {
							ROOT = { is_in_faction_with = NAT }
							FROM = { is_in_faction_with = SOV }
						}
					}
				}
				WW3_1950_starting_effect = yes
			}
		}
	}
}