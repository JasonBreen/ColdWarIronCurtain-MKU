###HIGUYS RAPED###for new coder who wanna learn on_action####
#on_action = { #<- main stuff always on_action
	#on_raped = {   #<- this is the on_action type, this one isnt real but you get the idea#
		#effect = { #<- this is effect bracket#
			#if = { #<- most on action uses "if" argument keep that in mind#
				#imit = { #<- this is the check for trigger(s)#
					#higuys_raped = yes #<- trigger#
					#higuys_raped_factor = > 0.05 #<- trigger#
					#ROOT = { #<- trigger (most onaction uses argument such as ROOT, FROM, THIS etc )#
						#original_tag = ITA #<- trigger for root
					#}
					#FROM = { #<- trigger (most onaction uses argument such as ROOT, FROM, THIS etc )#
						#original_tag = COS #<- trigger for from
					#}
				#} #<- end of trigger check#
				#ROOT = { #<- remember ROOT is Italy#
					#set_country_flag = higuys_raped #<- effect#
					#add_ideas = higuy_raped_and_killed #<- effect#
				#}
				#FROM = { #<- Costa Rica#
					#add_to_variable = { #<- effect with bracket, this is what usually are called scopes#
						#var = fabian_raped_var_factor = 0.05 
						#tooltip = fabian_raped_increased_TT
					#} 
					#set_country_flag = higuy_raped_fabian #<- effect#
					#declare_war_on = { #<- effect with bracket, this is what usually are called scopes#
						#target = ITA
						#type = to_rape_higuys
					#} #<- end of effect scope#
				#} #<- end of effect trigger#
			#} #<- end of effect if argument#
		#} #<- end of effect#
	#} #<- end of on_action argument#
#} #<- end of on_action