add_namespace = swfs

country_event = {
	id = swfs.10 
 immediate = {log = "[GetDateText]: [Root.GetTag]: id = swfs.10"}
	title = swfs.10.t
	desc = swfs.10.d

	is_triggered_only = yes

	option = {
		name = swfs.10.a
		set_autonomy = { target = LIT autonomy_state = autonomy_free }
		LIT = {
			set_politics = { ruling_party = monarchism elections_allowed = yes }
			add_ideas = monarchist_influence
		}
		set_autonomy = { target = BLR autonomy_state = autonomy_free }
		BLR = {
			set_politics = { ruling_party = monarchism elections_allowed = yes }
			add_ideas = monarchist_influence
		}
		set_autonomy = { target = UKR autonomy_state = autonomy_free }
		UKR = {
			set_politics = { ruling_party = monarchism elections_allowed = yes }
			add_ideas = monarchist_influence
		}
		ai_chance = {
			factor = 80
		}
	}

	option = {
		ai_chance = {
			factor = 20
		}
		name = swfs.10.b
		POL = { create_wargoal = { type = annex_everything target = SOV } }
	}
}country_event = {
	id = swfs.11 
 immediate = {log = "[GetDateText]: [Root.GetTag]: id = swfs.11"}
	title = swfs.11.t
	desc = swfs.11.d

	is_triggered_only = yes

	option = {
		name = swfs.11.a
			HUN = { transfer_state = 76 }
			HUN = { transfer_state = 84 }
			84 = { add_core_of = HUN }
			#82 = { add_core_of = HUN }
			#HUN = { transfer_state = 82 }
		ai_chance = {
			factor = 90
		}
	}

	option = {
		ai_chance = {
			factor = 10
		}
		name = swfs.11.b
		HUN = { create_wargoal = { type = annex_everything target = ROM } }
	}
}country_event = {
	id = swfs.12 
 immediate = {log = "[GetDateText]: [Root.GetTag]: id = swfs.12"}
	title = swfs.12.t
	desc = swfs.12.d

	is_triggered_only = yes

	option = {
		name = swfs.12.a
			HUN = { transfer_state = 45 }
		ai_chance = {
			factor = 90
		}
	}

	option = {
		ai_chance = {
			factor = 10
		}
		name = swfs.12.b
		HUN = { create_wargoal = { type = annex_everything target = YUG } }
	}
}