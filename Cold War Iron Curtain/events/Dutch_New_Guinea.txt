add_namespace = WPA

country_event = {
	id = WPA.1
	title = WPA.1.t  #Indonesia
	desc = WPA.1.d
	picture = GFX_WPA_1
    fire_only_once = yes
	is_triggered_only = yes
    immediate = {
        log = "[GetDateText]: [Root.GetTag]: id = WPA.1"
    }
	option = {
        name = WPA.1.a
        retire_character = WPA_van_Eechoud
        # recruit_character = WPA_van_Waardenburg  # Should be in history file; character role should be added in focus completion
    }
}
country_event = {
	id = WPA.2
	title = WPA.2.t  #Indonesia
	desc = WPA.2.d
	picture = GFX_WPA_1
    fire_only_once = yes
	is_triggered_only = yes
    immediate = {
        log = "[GetDateText]: [Root.GetTag]: id = WPA.2"
    }
	option = {
        name = WPA.2.a
        retire_character = WPA_Louis_Beel
        # recruit_character = WPA_van_Waardenburg  # Should be in history file; character role should be added in focus completion
    }
}