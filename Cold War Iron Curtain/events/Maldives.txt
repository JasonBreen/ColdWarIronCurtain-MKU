add_namespace = MLD_skeleton

country_event = {
    id = MLD_skeleton.1
    immediate = { }
    title = MLD_skeleton.1.t
    desc = MLD_skeleton.1.d
    #picture = 

    is_triggered_only = yes

    fire_only_once = yes

    option = {
        name = MLD_skeleton.1.a
		set_cosmetic_tag = MLD_firstrepublic
	}
}

country_event = {
    id = MLD_skeleton.2
    immediate = { }
    title = MLD_skeleton.2.t
    desc = MLD_skeleton.2.d
    #picture = 

    is_triggered_only = yes

    fire_only_once = yes

    option = {
        name = MLD_skeleton.2.a
		set_cosmetic_tag = MLD_sultanate2
		set_popularities = {
			conservative = 100
		}
		set_politics = {
			ruling_party = conservative
			last_election = "1952.11.11"
			election_frequency = 48
			elections_allowed = no
		}
	}
}

country_event = {
    id = MLD_skeleton.3
    immediate = { }
    title = MLD_skeleton.3.t
    desc = MLD_skeleton.3.d
    #picture = 

    is_triggered_only = yes

    fire_only_once = yes

    option = {
        name = MLD_skeleton.3.a
		set_popularities = {
			nationalist = 100
		}

		set_politics = {
			ruling_party = nationalist
			last_election = "1952.11.11"
			election_frequency = 48
			elections_allowed = no
		}
	}
}

country_event = {
    id = MLD_skeleton.4
    immediate = { }
    title = MLD_skeleton.4.t
    desc = MLD_skeleton.4.d
    #picture = 

    is_triggered_only = yes

    fire_only_once = yes

    option = {
        name = MLD_skeleton.4.a
		set_cosmetic_tag = MLD_kingdom
		overlord = { country_event = MLD_skeleton.5 }
	}
}

country_event = {
    id = MLD_skeleton.5
    immediate = { }
    title = MLD_skeleton.5.t
    desc = MLD_skeleton.5.d
    #picture = 

    is_triggered_only = yes

    fire_only_once = yes

    option = {
        name = MLD_skeleton.5.a
		set_autonomy = { target = MLD autonomy_state = autonomy_free }
	}
}

country_event = {
    id = MLD_skeleton.6
    immediate = { }
    title = MLD_skeleton.6.t
    desc = MLD_skeleton.6.d
    #picture = 

    is_triggered_only = yes

    fire_only_once = yes

    option = {
        name = MLD_skeleton.6.a
		drop_cosmetic_tag = yes
		set_popularities = {
			neutrality = 100
		}
		set_politics = {
			ruling_party = neutrality
			last_election = "1968.9.27"
			election_frequency = 60
			elections_allowed = no
		}
	}
}

country_event = {
    id = MLD_skeleton.7
    immediate = { }
    title = MLD_skeleton.7.t
    desc = MLD_skeleton.7.d
    #picture = 

    is_triggered_only = yes

    fire_only_once = yes

    option = {
        name = MLD_skeleton.7.a
		USR = {
			transfer_state = 1897
			on_independence_run = yes
		}
	}
}

country_event = {
    id = MLD_skeleton.8
    immediate = { }
    title = MLD_skeleton.8.t
    desc = MLD_skeleton.8.d
    #picture = 

    is_triggered_only = yes

    fire_only_once = yes

    option = {
        name = MLD_skeleton.8.a
		annex_country = { target = USR transfer_troops = yes }
	}
}