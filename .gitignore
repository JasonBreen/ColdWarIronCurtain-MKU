# =========================
# Operating System Files
# =========================
Thumbs.db
ehthumbs.db
ehthumbs_vista.db
.DS_Store
Desktop.ini
$RECYCLE.BIN/

# Windows Installer files
*.cab
*.msi
*.msm
*.msp

# Windows shortcuts
*.lnk

# =========================
# Editor / tooling
# =========================
# Personal overrides. The shared defaults in .vscode/settings.json and
# .vscode/extensions.json ARE tracked on purpose — point the HOI4 Mod Utilities
# extension at your local install from your own user settings, not from here.
.vscode/settings.local.json
.idea/

# Claude Code local overrides (.claude/settings.json is tracked on purpose)
.claude/settings.local.json

# =========================
# Python tooling
# =========================
__pycache__/
*.py[cod]
.venv/
venv/

# =========================
# Repo-specific
# =========================
Cold War Iron Curtain/gfx/SyncToy_ea3e2603-e5ec-4e10-a9ed-c5472a69effa.dat
~$tableforswf.xlsx

# HOI4 writes these next to the mod; never commit them.
*.log
logs/
# ...except the archived copy that is already tracked.
!CWIC Backup/**/*.log

# NOTE: `Cold War Iron Curtain/descriptor.mod` was previously listed here, but
# it is tracked and the mod cannot load without it. Its `path=` line is
# developer-local by design — leave that line alone rather than ignoring the
# whole file. See AGENTS.md.
